schema_version: 1
inbounds:
  - type: mixed
    tag: "mixed-in"
    listen: "127.0.0.1"
    listen_port: 1080
outbounds:
  - type: direct
    tag: direct
route:
  rules:
    - domain_suffix:
        - "example.com"
      outbound: direct