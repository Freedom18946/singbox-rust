# Rule Fragments

Files in this directory are rule fragments and snippets, not full service configs.

Kept files:

- `basic-router.rules`
- `snippets/block_ads.dsl`

Use them as source material for rule-set generation or DSL experiments. If you need a full runnable routing config, use `examples/configs/routing/rules_demo.json` instead.
