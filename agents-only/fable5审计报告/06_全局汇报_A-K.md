<!-- tier: B -->
# REPO-GLOBAL-CALIBRATION-01A 全局汇报(A–K 完整版)

> 主线程综合报告 | 2026-06-10 | HEAD `02d8d16e` | audit-only(零 tracked 修改、零 commit、零 push)
> 输入:Phase 1 文档地图(主线程)+ 5 个并行 opus agent(R-A/R-B1/R-B2/R-C/R-D,原文见本目录 01–05)+ Phase 3 主线程对全部 P0/核心 P1 证据的逐条 `文件:行号` 复核。
> 证据标注:`[✓主线程复核]` = 我本人打开文件/运行命令验证过;其余为 agent 取证(全部抽验未发现幻觉)。

---

## A. Push 结果

| 项 | 值 |
|---|---|
| push 前 HEAD(计划期核验) | `02d8d16e5061e4d7fb60bf30e2899d2099000acf` |
| push 前 origin/main(本地 ref) | `02d8d16e`(**与任务书预期 `907f5944` 不符**) |
| `git fetch origin` 后复核 | origin/main = `02d8d16e` |
| `git ls-remote origin main`(远端真值) | `02d8d16e5061e4d7fb60bf30e2899d2099000acf refs/heads/main` [✓主线程复核] |
| push 的 commit 范围 | **无**(未执行 push) |
| push 输出摘要 | N/A |
| push 后 HEAD / origin/main | `02d8d16e` / `02d8d16e`(目标状态已达成) |
| 最终 `git status --short --branch` | `## main...origin/main` + `?? agents-only/a0_reality_spike/`(后续新增 `?? agents-only/fable5审计报告/` 为用户显式授权产物) |
| `agents-only/a0_reality_spike/` | **全程未触碰**(未暂存/提交/删除/移动/修改) |

**偏差说明**:任务书预期"本地 ahead 2、origin=907f5944、需 push"。实际核验发现 `47e15b0b` + `02d8d16e` **已在先前会话 push 完成**(`git log --decorate` 显示 `origin/main, origin/HEAD` 落在 02d8d16e;`ls-remote` 远端真值一致)。任务书要求的最终状态(`HEAD = origin/main = 02d8d16e`)已成立,故按计划(用户已批准)记录偏差、跳过 push、进入 audit 阶段。`git diff --check` 干净,tracked tree 干净。

---

## B. 审计范围地图

| 区域 | 扫描深度 | 说明 |
|---|---|---|
| `app/src/` 运行时面(bootstrap/run_engine*/sidecar_runtime/inbound_starter/cli/bin) | **完整** | R-B1/R-C 全文阅读 supervisor 链路;26 个 bin 入口由 R-A 清点 |
| `crates/sb-core/runtime/supervisor.rs`(2734 行) | **完整** | R-B1 全文 + R-C 场景追踪 + 主线程复核关键行 |
| `crates/sb-core/`(context/adapter/bridge/registry/services/endpoint/dns/router) | **完整(生命周期面)/ 抽样(协议细节)** | bridge/registry/context 全读;router/dns 按能力域抽样 |
| `crates/sb-adapters/`(register.rs/inbound/outbound) | **完整(注册与生命周期)/ 抽样(协议实现)** | register.rs stub/真实分支逐协议核对;TUN 模块全读 |
| `crates/sb-config/`(validator/v2、ir) | **完整(inbound/dns/top_level)/ 抽样(其余)** | TUN schema 断裂主线程复核 |
| 其余 crates(sb-tls/transport/api/platform/...) | **抽样** | RB2 危险模式全 workspace 扫描 + clash server 路由表全读 |
| 测试体系(~4307 测试函数 / 153 app test 文件 / xtests / labs) | **抽样 + 实跑** | rollback/clash_api/trojan/dns 实跑;统计学抽样 |
| 门禁(boundaries/clippy/fmt) | **实跑验证** | boundaries exit 0(537 断言)、clippy all-features 0 warn 均实测 |
| `GUI_fork_source/GUI.for.SingBox-1.25.1/` | **针对性完整**(内核契约面) | kernel.ts/kernelApi.ts/generator.ts/profile.ts/enums 全读 [✓主线程复核 kernel.ts:17] |
| `go_fork_source/sing-box-1.13.13/` | **对照点抽样** | box.go 启动输出、srs binary.go、option 结构对照 |
| docs/ + reports/ + agents-only/ 文档 | **完整(可信度维度)** | R-D 漂移扫描 + 主线程 S/A-tier 全读 |
| **未能确认** | — | 协议字节级行为对齐(双核差分领域,本轮不重跑)、Windows/Linux 实机、REALITY 实网、sb-core 1109 全量测试数(抽样推断)、GUI 实机拉起(未运行 GUI) |

---

## C. 权威文档与最高目标

### C.1 权威文档优先级(经可信度复核)

| 优先级 | 文档 | 性质 | 可信度结论 |
|---|---|---|---|
| 1 | `agents-only/active_context.md` | 易变状态唯一权威 | **高度可信**(R-D 逐项抽验吻合,含主动登记自身缺口) |
| 2 | `labs/interop-lab/docs/dual_kernel_golden_spec.md` | 行为对齐权威 | 可信(本轮未重跑差分) |
| 3 | `agents-only/workpackage_latest.md` | 阶段地图 | 可信 |
| 4 | `agents-only/reference/GO_PARITY_MATRIX.md` | 验收基线 | 可信但口径易误读(209/209=验收口径,行为对齐≈183/209;锚点部分过期且自我标注) |
| 5 | `docs/capabilities.md` + `reports/capabilities.json` | 能力三态台账 | **陈旧 335 commits**(自标 snapshot_unverified,危害有节制) |
| 6 | `docs/STATUS.md` | 状态入口 | 可信(对冲得当) |
| — | `README.md`、`docs/TEST_EXECUTION_SUMMARY.md`、`docs/CLEANUP_COMPLETION_REPORT.md` | 外宣/历史 | **已漂移**(见 CAL-26) |

### C.2 最高目标(≤200 字)

**交付一个可直接替换 Go sing-box 1.13.13 的 Rust 二进制,与 GUI.for SingBox(1.25.1)完全兼容。** 核心用户价值:GUI 用户把 Rust 内核放进 GUI 的内核目录后**无感**使用——配置可解析、进程握手可识别、TUN/系统代理模式可用、节点选择与持久化照常、reload 不致断网。当前最接近的可交付里程碑:**GUI 实机拉起 Rust 内核走通主流程**(导入订阅 → 启动内核 → 选节点 → TUN 或系统代理模式可上网 → 切换配置不断网)。

### C.3 真正 DoD(而非"局部代码存在")

1. GUI 生成的默认配置(含 TUN)被 schema 接受并启动成功;
2. GUI 进程握手成立(启动确认输出、版本门控、stop 语义);
3. 主流协议数据面 + TUN 数据面真实转发;
4. reload/切换配置不造成无信号的服务空洞;
5. 上述各项有行为级证据(真实 GUI 或等价 harness),非 closure 矩阵文字闭环。

### C.4 文档冲突与模糊点

1. **README "Maintenance mode" vs 用户最高目标**(memory 已记录 "maintenance ≠ done"):对外信号与实际方向矛盾。
2. **"209/209 closed" 验收口径**极易被误读为行为对齐(矩阵头部虽有标注,但 README/STATUS 的引用处无此对冲)。
3. **MT-GUI-04 "55/55 能力验收"** 与本轮发现的 RA-01/RA-02 两个 GUI P0 并存——强烈暗示该验收**未包含"真 GUI 进程拉起内核"路径**(用的是双内核+mock 公网逐项测试)。验收口径与 DoD 之间存在系统性缝隙。
4. **`reports/capabilities.json` 陈旧 335 commits**,未登记 WG endpoint 孤岛、TUN schema 断裂。
5. `supervisor.rs:12` 文档承诺 "Hot Reload without dropping connections / Diffing" 与实现(全量重建)直接矛盾 [✓主线程复核 step0 代码]。

---

## D. 全局完成度校准

| 子系统 / 能力 | 对最高目标的作用 | 实现证据 | 测试证据 | 当前缺口 | 完成度区间 | 置信度 |
|---|---|---|---|---|---|---|
| GUI 进程契约(启动握手/版本/stop) | **阻塞性**(GUI 拉起内核的第一关) | output.rs 输出 `singbox-rust booted`;version 0.1.0 | 无 GUI 实机测试 | CAL-02 启动字符串、CAL-17 版本门控 | **30%–50%** | 高 |
| 配置 schema 兼容(Go 1.13.13 字段) | **阻塞性** | validator/v2 严格校验,主流 outbound/DNS 可解析 | golden_go1124_roundtrip 等 18 文件 | CAL-01 TUN 现代字段全缺;严格拒绝 unknown field 放大任何字段缺口 | **55%–70%** | 高 |
| TUN 数据面 | **阻塞性**(GUI 主模式) | tun/mod.rs skeleton;唯一真实路径 smoltcp | tun_enhanced 有 connect/relay 测试 | CAL-01+CAL-10:schema 拒绝 + 默认 stack no-op + GUI stack 枚举不识别 | **15%–30%** | 高 |
| WireGuard(endpoint + outbound) | 重要(现代配置常见) | endpoint build 成功但孤岛;outbound 永 stub | 无 run 路径测试 | CAL-03 + CAL-09 | **10%–25%** | 高 |
| 主流协议 outbound(vmess/vless/trojan/ss/hy2/tuic/ssh…) | 核心 | register.rs 真实注册,vmess 实测启动 | 协议 integration 测试(部分最深 ignore) | 长尾 stub(CAL-18);最深握手测试 ignore | **80%–90%** | 高 |
| inbound(socks/http/mixed) | 核心 | 实测绑定+转发 | 真 listener 测试充分 | bind 失败不可见(CAL-05) | **85%–90%** | 高 |
| 路由引擎(rule-set/geo/process/sniff) | 核心 | SRS 解析对齐 Go;全量在册 | route_explain E2E | 未逐规则行为验证 | **75%–85%** | 中高 |
| DNS(12 传输 + 规则 + fakeip) | 核心 | 全部真实 from_spec,实测启动 | A/B 混合 | resolver 构建失败静默丢(CAL-16);fakeip mask 静默(CAL-22) | **75%–85%** | 中高 |
| clash_api 管理面(GUI 依赖) | 核心 | 端点全覆盖,实测 PUT/delay/WS | clash_http_e2e 真实 HTTP | API down 无机器可读信号(CAL-13) | **85%–95%** | 高 |
| 节点选择持久化(cache_file) | 核心(GUI 依赖) | sled selected tree 实测落盘 | 持久化测试(1 flake) | 与 Go BoltDB 格式不互通(未知项 H-7) | **80%–90%** | 高 |
| reload / 生命周期可靠性 | 重要(drop-in 可靠性面) | rollback 事务 + V2Ray 复用闭环 | 9+1 rollback_guard 实跑 PASS | **CAL-04 连续性簇 + CAL-05 + CAL-07**(三项叠加 = 失败 reload 即断网且可被成功信号掩盖) | **55%–70%** | 高 |
| 可观测性(inbound liveness / API down / 静默降级) | 重要 | sidecar 有监控分类 | classify 测试 | CAL-06(主数据面零监控)、CAL-13、CAL-16 | **40%–60%** | 高 |
| 测试/门禁体系 | 支撑 | boundaries 537 断言 exit 0 实测;clippy 0 warn 实测 | ~4307 测试,A 类为主 | CI 空缺(已知决策);lints 死配置(CAL-08);局部腐烂(CAL-19) | **75%–85%** | 高 |
| 文档体系 | 支撑 | 内部 agents-only 高度可信 | — | 外部 docs/README 漂移簇(CAL-26) | 内部 **85%–95%** / 外部 **50%–65%** | 高 |

### 加权总评

```text
全仓面向最高目标的加权完成度区间:65%–75%
置信度:中高
判断依据:
- 管理面与主流协议面(clash_api、选择持久化、socks/http/mixed、主流 outbound、DNS、路由)
  真实闭环且经实测,占工程体量的大头 → 拉高基线;
- 但按"是否阻塞最高目标"加权:GUI 主流程的三个入口闸门(配置 schema、进程握手、TUN
  数据面)全部断裂(3×P0),WG 双形态不可用,reload 可靠性面有结构性缺口(P1 簇)——
  这些不是体量大的工作,但全部压在 drop-in 的必经路径上;
- "92.9% BHV / 209/209 closed / 55/55 验收"三轴均不度量"GUI 真实拉起",本轮发现
  恰好落在三轴覆盖之外的缝隙里,故各轴数字与本区间不矛盾,但都不能替代它。
```

---

## E. 已确认完成的关键能力(真正闭环,附证据)

1. **clash_api 全套 GUI 端点**:`crates/sb-api/src/clash/server.rs:280-349` 路由表;实测 `PUT /proxies/:name`→204、selection 生效、WS traffic/logs。
2. **节点选择持久化**:`crates/sb-core/src/services/cache_file.rs:185,284-286` sled `selected` tree,实测落盘。
3. **主流协议 outbound 注册装配**:register.rs 真实分支 + vmess happy-path 实测(`Bridge components registered`)。
4. **mixed/socks/http inbound 转发链**:实测绑定+转发。
5. **DNS 12 传输真实实现**:GUI 风格 DNS 配置实测启动。
6. **SRS 二进制 rule-set 解析**:`router/ruleset/binary.rs:51-75` magic/version 校验对齐 Go。
7. **pre-swap rollback 事务(01B)**:`supervisor.rs:625-676` 单事务块 + `shutdown_failed_reload_context`;9 个 rollback_guard 测试实跑 PASS(真实 listener、端口可重绑定、重复失败零累积)[✓R-D 实跑]。
8. **V2Ray 跨 reload 复用(01C)**:`reusable_v2ray_server:1318-1341` 严格门控 + ptr_eq 区分 fresh/inherited;测试 A–L 覆盖。
9. **shutdown/reload 串行化**:单 mpsc 事件循环,无双消费者,无死锁路径(场景 12)。
10. **协议解析 panic 安全**:trojan/vless/vmess/ss/socks/ssr 解析路径零可被恶意对端触发的 panic/unwrap(R-B2 全量过滤审计)。
11. **坏配置不崩溃**:validate_v2 严格校验前置,parse 级错误在 reload 中于 step0 之前被拒(场景 17)——运行中服务不受波及。
12. **边界门禁**:check-boundaries.sh exit 0,V7 537 断言 0 违规 [✓R-D 实跑]。
13. **clippy all-features all-targets 0 warn** [✓R-D 实跑]。
14. **sidecar 终态分类**:Clash/V2Ray 退出被 classify(Panicked/ServeError/…)且 `#[non_exhaustive]` 安全降级。
15. **进程匹配跨平台**:linux/macos/windows/android 全真实现,非支持平台 loud-fail。

---

## F. 已确认缺陷与明显缺口(去重合并后)

> 编号 CAL-nn;来源 agent 发现号在括号内。P0/P1 全部经主线程二次复核。

### P0(阻塞交付)

**CAL-01 GUI TUN 配置被 schema 拒绝,内核无法启动**(RA-01)
- 类型:MISSING FEATURE / SEMANTIC GAP|状态:**CONFIRMED** [✓主线程复核:`validator/v2/inbound.rs:8-34` allowed keys 无 mtu/address/stack;`ir/inbound.rs:449` InboundIR 17 字段同样无之]
- 证据:`crates/sb-config/src/validator/v2/inbound.rs:8-34`;`crates/sb-config/src/lib.rs:520-537`(无条件 strict);GUI 生成器 `GUI_fork_source/.../utils/generator.ts:81-90`。agent 实测:`address`→`mtu`→`stack` 逐个被拒。
- 触发:GUI 默认 TUN profile。影响:**GUI 主模式当场无法启动**。
- 为什么现在重要:它是 drop-in 的第一道门;且暴露系统性风险——strict schema + 不全的字段表 = 任何未登记 Go 字段都硬失败。
- 最小验证:`app run -c <GUI 风格 tun 配置>`。
- 建议方向:扩 InboundIR/allowed keys 至 Go 1.13.13 TUN 字段集;同时系统性比对 Go option 结构(见 PKG-SCHEMA-PARITY-01)。

**CAL-02 启动确认字符串 `sing-box started` 缺失,GUI 判定启动失败**(RA-02)
- 类型:SEMANTIC GAP(进程契约)|状态:**CONFIRMED** [✓主线程复核:GUI `constant/kernel.ts:17` + `stores/kernelApi.ts:257` 依赖该子串;Go `box.go:434` 输出;Rust 全仓 grep 0 命中]
- 触发:GUI 拉起内核(所有模式)。影响:GUI 永不 resolve 启动 Promise → 即使内核已运行也判失败。**比 CAL-01 更普遍**。
- 最小验证:`app run … 2>&1 | grep 'sing-box started'`(空)。
- 建议方向:在 supervisor 启动完成点输出兼容行(注意与 CAL-05 联动:输出时机应在 bind 确认后,否则是假 ready)。

**CAL-03 WireGuard endpoint 孤岛:建好但永不进 outbound 命名空间 → 硬启动失败**(RA-03)
- 类型:BUG(接线缺失)|状态:**CONFIRMED** [✓主线程复核:`EndpointAsOutbound`(endpoint/mod.rs:588-624)全仓零生产调用;bridge.rs Step 4 仅 `add_endpoint`]
- 触发:配置含 wireguard endpoint 且路由指向其 tag。影响:agent 实测 `Error: default outbound not found: wg-ep` 硬失败。
- 最小验证:已实测。建议方向:bridge Step 4 中将 endpoint 以 `as_outbound_connector` 注入 outbound map。

### P1(阻塞近期最高目标 / 显著削弱可靠性)

**CAL-04 reload 连续性簇:step0 提前关旧 + 全量无 diff + 失败无恢复 + 信号竞态**(RC-01、RD-01、RB1-01、RB1-02;= 种子 01A 升级)
- 类型:SEMANTIC GAP / ARCHITECTURE DEBT|状态:**CONFIRMED**(竞态子项 HIGH-CONFIDENCE)[✓主线程复核 step0 `supervisor.rs:572-577` + grace + `Engine::from_ir` 在其后]
- 四个子面:① step0 在激活前无条件 `request_shutdown` **全部**旧 inbound(:575-577);② 无 diff,任何配置变化全端口重建(与 `supervisor.rs:12` 文档承诺直接矛盾);③ 激活失败 → 回滚新资源但**旧 inbound 永不重启**(run_engine_runtime/supervisor.rs:355-367),失败 reload = 硬断网无恢复;④ stop_tx 注册竞态可使 request_shutdown 静默丢失(register.rs:3705-3728)。
- 影响:GUI 用户日常 reload;坏 apply = 断网直到下次成功 apply 或重启进程。Go 是 build-new-before-teardown 语义。
- 最小验证:SIGHUP 一个 validation-pass-but-activation-fail 的配置,观察旧端口停 accept 且不恢复。
- 建议方向:重排 reload 顺序为 build→bind(新端口集)→swap→teardown 旧;同端口场景以 bind-handoff 或 SO_REUSEADDR 处理;diff 增量留作二期。

**CAL-05 inbound bind/serve 失败不可见、不致命:启动与 reload 双路径**(RC-02、RC-04、RB1-07)
- 类型:BUG / SEMANTIC GAP|状态:**CONFIRMED** [✓主线程复核:activation 块内 `spawn_blocking(ib.serve())` handle 丢弃、Err 仅 `tracing::error!`(:662),不进 `activation_result`]
- 影响:端口被占时进程照样报 "started" / reload 照样 swap 成功 → 服务空洞被成功信号掩盖。`ready_tx` 形参恒传 `None`(基础设施已在、从未接线)。
- 最小验证:外部占用 1080 后启动 mixed inbound。
- 建议方向:接通 `ready_tx`,把 bind 确认纳入启动/激活判据(与 CAL-02 的输出时机联动)。

**CAL-06 inbound 主数据面零 liveness 监控:panic = 端口静默死亡**(RC-03)
- 类型:ARCHITECTURE DEBT / 可观测性|状态:**CONFIRMED**
- 证据:supervisor.rs:228/402/659/829 JoinHandle 全丢弃;对照 sidecar 有完整 classify 监控(admin_start.rs:277-302)——主数据面反而是观测最弱组件。
- 建议方向:为 inbound serve 任务加 monitor(分类记录 + metrics + 可选 restart 策略)。

**CAL-07 全局 RUNTIME_OUTBOUNDS/INBOUNDS 在 pre-swap 安装、回滚不恢复**(RB1-04)
- 类型:BUG / STALE GLOBAL STATE|状态:**CONFIRMED** [✓主线程复核:bridge.rs:768/836/909/945 build 期 install;supervisor.rs 全文 grep `install_runtime|RUNTIME_OUTBOUNDS` **0 命中** → 回滚路径确无恢复;detour.rs:23 热路径消费]
- 影响:reload 失败回滚后,detour 拨号解析到已废弃 bridge 的注册表;pre-swap 窗口内同样错位。不在 01A/01B/01C 任何已完成工作的覆盖范围内。
- 建议方向:install 移到 swap 成功点之后,或回滚时重装旧 bridge 的 handle。

**CAL-08 workspace 安全 lint(deny unwrap/expect/panic)对 15/16 crate 是死配置**(RB2-01)
- 类型:ARCHITECTURE DEBT(流程护栏)|状态:**CONFIRMED** [✓主线程复核:`[lints] workspace=true` 全仓唯一命中 sb-tls:76-77;workspace 根 47-51 定义 deny]
- 影响:当前代码经 R-B2 审计实际健壮(零生产 panic 路径),但护栏不存在 → 漂移风险。修复是 POLICY 决策(会暴露 2493 个存量 unwrap 待豁免/改造)。

**CAL-09 WireGuard outbound(legacy type)恒 stub:`adapter-wireguard-outbound` feature 无人启用**(RA-04)
- 类型:MISSING FEATURE|状态:**CONFIRMED**(parity 二进制 strings 实测无模块)
- 建议方向:与 CAL-03 同包处理(WG 双形态闭环)。

**CAL-10 TUN 数据面 skeleton:默认 Manual no-op,GUI 的 system/gvisor/mixed 枚举不被识别**(RA-05)
- 类型:MISSING FEATURE / SEMANTIC GAP|状态:**CONFIRMED**
- 证据:`tun/mod.rs:172-190` parse_stack_backend 仅认 manual/system/default→Manual(no-op)、smoltcp→Enhanced,`mixed/gvisor`→InvalidInput;macOS Manual 路径"仍然丢弃负载"(:526)。
- 建议方向:stack 名映射(gvisor/mixed→smoltcp 等价栈)+ Enhanced 路径真实流量验证;与 CAL-01 是同一交付链的两环。

### P2(重要,不阻塞最近里程碑)

| ID | 标题 | 类型 | 状态 | 关键证据 |
|---|---|---|---|---|
| CAL-11 | 生产路径残留 `eprintln!`(reload 必触发 + 连接热路径) | BUG(观测污染) | CONFIRMED | register.rs:3724、http.rs:308、tuic.rs:847、hysteria2.rs:544(RB1-03) |
| CAL-12 | graceful shutdown drain 形同虚设(嵌套 rt drop abort 活动连接) | SEMANTIC GAP | HIGH-CONF | handle_shutdown:1007 先停 → rt drop(RB1-05) |
| CAL-13 | Clash API down 仅一条 error log,无机器可读信号;Clash 在 reload 体系之外 | 可观测性 | CONFIRMED | admin_start.rs:341-411;sidecar_runtime.rs:325(RC-05 + 场景 8/13) |
| CAL-14 | 无 SO_REUSEADDR;同端口 handoff 依赖固定 1200ms grace 启发式 | reliability | CONFIRMED | mixed.rs:58 等 + supervisor.rs:591-597(RC-06) |
| CAL-15 | V2Ray 快速 disable→enable 同端口可瞬态 EADDRINUSE → API 静默缺席 | reliability | HIGH-CONF | wire_experimental_sidecars:1289 warn+skip(RC-07) |
| CAL-16 | resolver_from_ir 失败静默跳过(无 else 分支无日志) | 可观测性 | CONFIRMED | supervisor.rs:204-208/648-652(RC-08) |
| CAL-17 | 内核版本号 0.1.0 可能触发 GUI 版本门控降级 | 兼容性 | HIGH-CONF | GUI 按 1.10/1.11/1.12 分支启用字段(RA-07) |
| CAL-18 | tor/tailscale/dns/shadowsocksr outbound parity 下 stub | MISSING FEATURE | CONFIRMED | register.rs cfg-not 分支 + strings 实测(RA-06) |
| CAL-19 | selector/proxy-pool 测试腐烂 ~7.5 月(4 条 ignore "API changed") | TEST GAP | CONFIRMED | proxy_pool_select.rs/selector_p2.rs(RD-06) |

### P3(卫生 / 低风险债)

| ID | 标题 | 来源 |
|---|---|---|
| CAL-20 | ServiceManager::close no-op 是手工对齐而非结构保证(8 处补偿齐全,泄漏推论已证伪) | RB1-08/RB2-02 |
| CAL-21 | FakeIP 畸形掩码静默丢弃(`.ok()` 无校验) | RB2-03 |
| CAL-22 | `experimental` 块解析失败静默置空 | RB2-04 |
| CAL-23 | system_proxy 非支持平台假成功;macos 回调吞错 | RB2-05 |
| CAL-24 | serve_http 心跳 task 在共享 runtime 路径泄漏(生产 A 路径已豁免) | RB1-06 |
| CAL-25 | 三条运行时入口并存,bootstrap.rs `start_from_config` 零生产调用(死代码层) | RB1 地图 |
| CAL-26 | 外部文档漂移簇:capabilities.json(335c)/README 头牌数字/TEST_EXECUTION_SUMMARY/CLEANUP_REPORT | RD-02/03/04/05 + RA-08 |
| CAL-27 | 特定 feature 下 cargo test 2 warn(unused import / dead fn) | RD-07 |
| CAL-28 | trojan 最深真连接测试 #[ignore] | RD-09 |
| CAL-29 | flake 三件套根因(环境/时序/fs)+ `bad_domain_returns_err` 对劫持网络假失败 | RD flake 分析 |

---

## G. 被推翻或需要降级的旧判断

1. **种子 `APP-RELOAD-INBOUND-CONTINUITY-01A`:DEFER → 应升级为活动 P1**。三个独立 agent(R-B1/R-C/R-D)交叉确认其成立且**被低估**:真实影响面 = 提前关旧 + 新 inbound 启动不在成功判据(CAL-05)+ 全量无 diff + 失败无恢复,合并为 CAL-04 簇。
2. **"ServiceManager::close no-op ⇒ 服务会泄漏" 的隐含推论:DISPROVED**。8 处关闭路径全部有显式 `stop_services` 补偿(R-B2 grep 11 处核验)。问题降级为 P3 结构债(手工对齐风险),非行为缺陷。
3. **`SVC-V2RAY-API-01B`:维持 DEFER,确认无行为缺陷**。复用与回滚判别实现正确且测试齐全;唯一边缘 RC-07(P2)。
4. **`APP-SIDECAR-POLICY-02A`:维持 DEFER,但应附加具体化的 RC-05/CAL-13**(ownership 缺口的可观测落点)+ "Clash 在 supervisor reload 体系之外" 的结构事实。
5. **`TIDY-RUSTDOC-LINKS` / `TIDY-APP-BREAKER-FLAKE`:维持低优**(R-D 复核 baseline 稳定/隔离即过)。
6. **"MT-GUI-04 55/55 ⇒ GUI 兼容已闭环" 的隐含结论:被推翻**。CAL-01/02 两个 GUI 主流程 P0 存活至今,证明该验收不含真 GUI 进程拉起路径。验收数字本身不必改,但**不能再作为 "GUI-ready" 的证据引用**。
7. **"全局状态 reload 不安全" 的泛化担忧:大部分 DISPROVED**(R-B2 全局清单逐项评估,唯一真问题是 CAL-07 的安装时机,已单独登记)。
8. **"0 warn" 声明:需限定口径**——仅对 clippy-all-features 成立;`cargo test -p sb-core --lib` 有 2 warn(CAL-27)。
9. **"协议解析路径可能有 panic 风险" 的常规假设:DISPROVED**(R-B2 全量过滤:2493 unwrap/758 expect/125 panic 中,生产可触发路径 0)。
10. **优先级总排序被推翻**:此前隐含 "下一步 = 清 DEFER 列表";本轮证明 5 个已登记种子中只有 01A 阻塞最高目标,而**未登记的 CAL-01/02/03 三个 P0 比全部种子都重要**。

---

## H. 最危险的未知项(≤10)

| # | 未知点 | 为什么危险 | 当前证据 | 需要的 probe |
|---|---|---|---|---|
| 1 | **GUI 实机从未拉起过 Rust 内核**(疑似) | 两个 P0(CAL-01/02)能存活至今 ⇒ 可能还有第三、第四个未被任何测试轴覆盖的契约断裂 | MT-GUI-04 用 mock 公网;无 GUI 实机记录 | 真 GUI + Rust 内核端到端一跑(PKG-GUI-E2E-01) |
| 2 | GUI 是否使用热 reload(还是切换配置即重启内核进程) | 决定 CAL-04 的真实暴露面:若 GUI 总是重启进程,连续性簇降级;若用 clash_api PUT /configs,则是日常路径 | 未读 GUI 重启逻辑细节 | 读 GUI kernelApi 配置切换代码路径 |
| 3 | 版本号 0.1.0 在 GUI 版本门控的实际行为 | 可能使 GUI 按 <1.10 行为裁剪字段/功能,造成隐性降级 | GUI 有 1.10/1.11/1.12 分支(RA-07) | 读 GUI getKernelVersion 比较逻辑 + 实测 |
| 4 | strict schema 还拒绝多少 Go 1.13.13 合法字段(TUN 之外) | CAL-01 暴露的是系统性模式:unknown field = 硬失败;其它 inbound/outbound/route 字段可能同样缺 | 仅 TUN 已证实 | 系统性 diff Go option 结构 vs allowed keys(PKG-SCHEMA-PARITY-01 内置) |
| 5 | TUN smoltcp(Enhanced)路径真实流量健壮性 | 即使 schema/映射修复,数据面若不稳则 GUI 主模式仍不可用 | 有 connect/relay 测试但模块自标 skeleton | 真实 TUN 设备 + 真流量压测 |
| 6 | cache_file sled vs Go BoltDB 的 cache.db 格式不互通 | 用户从 Go 迁移时选择/fakeip 状态丢失或启动报错 | Rust 用 sled(已证);Go 用 BoltDB | 用 Go 生成的 cache.db 启动 Rust 内核观察行为 |
| 7 | sb-core "1109 PASS" 全量真值 | 文档声明仅抽样佐证(HIGH-CONF 推断) | R-D filtered 计数自洽 | 一次全量 `cargo test -p sb-core` |
| 8 | Windows/Linux 平台路径实测空白 | 平台 cfg 分支多;macOS 之外只有代码级证据 | R-B2 平台一致性表 | 目标平台 CI 或实机冒烟 |
| 9 | Go reload 语义的精确对照(diff 粒度/连接保持) | CAL-04 修复设计需要权威对照锚点,避免"修成另一种不对齐" | supervisor.rs 文档自称 diff(未实现);Go 侧未细读 | 读 go_fork_source box reload + GUI 调用面 |
| 10 | 订阅(sb-subscribe)对主流机场格式的真实覆盖 | GUI 主流程第一步是导入订阅;本轮未深扫 | R-A 标注 55-70% 区间含此不确定性 | 主流订阅样本回归集 |

---

## I. 建议任务包(10 个)

> 不含实现代码;每包可独立验证、可回滚、边界清晰。ID 沿用仓库卡片风格。

**PKG-GUI-CONTRACT-01** — GUI 进程契约修复
- 目标:Rust 内核被 GUI 识别为成功启动的 sing-box。覆盖:CAL-02(启动字符串)、CAL-17(版本策略,probe 后定)。
- 为什么现在:最小成本解锁最大验证面;不解决它,一切 GUI 端验证无法开始。
- 范围边界:`app/src/run_engine_runtime/output.rs` + 启动完成点;version 字符串策略。
- 明确禁区:不动 reload 逻辑;不假 ready(输出时机需等 CAL-05 的 bind 确认或先以 supervisor-started 为准并在文档中标注语义)。
- 前置依赖:无。可并行:与 PKG-SCHEMA-PARITY-01 并行。
- 验收:`app run` stdout 含 `sing-box started`;GUI(或等价脚本)启动 Promise resolve。
- 门禁:现有全套 + 新增输出断言测试。风险:低。

**PKG-SCHEMA-PARITY-01** — 配置 schema 字段对齐(TUN 优先 + 系统性扫描)
- 目标:GUI 默认 TUN 配置通过校验;产出 Go option 结构 vs allowed-keys 的全字段差异清单。覆盖:CAL-01、未知项 #4。
- 范围:`sb-config` validator/v2 + ir(InboundIR TUN 字段);diff 报告。
- 禁区:不在本包接 TUN 数据面(那是 PKG-TUN-DATAPLANE-01);不放松 strict 校验原则。
- 前置:无。可并行:与 PKG-GUI-CONTRACT-01。
- 验收:GUI 风格 TUN 配置 `app check`/`app run` 过校验;golden 测试覆盖新字段;差异清单归档。
- 风险:字段语义映射错误 → 用 Go 源结构逐字段对照。

**PKG-WG-DATAPLANE-01** — WireGuard 双形态闭环
- 目标:endpoint 作为路由目标可解析(CAL-03);评估并决策 legacy outbound feature 启用(CAL-09)。
- 范围:bridge.rs Step 4 接线 `as_outbound_connector`;app feature 表。
- 禁区:不重写 WG 协议栈;boringtun 限制维持已登记口径。
- 前置:无。可并行:可。
- 验收:`endpoints:[wireguard]` + route.final 指向 → 启动成功且可拨号(或明确登记运行时限制);agent 实测用例转为回归测试。
- 风险:endpoint→outbound 适配的 UDP 语义;先 TCP 通路。

**PKG-RELOAD-CONTINUITY-01** — reload 连续性重构(吸收种子 01A,升级为活动 P1)
- 目标:失败 reload 不再造成无恢复断网;成功 reload 的 bind 失败不再被掩盖。覆盖:CAL-04 全簇、CAL-05、CAL-07、CAL-14。
- 范围:`supervisor.rs` handle_reload/handle_reload_no_router 顺序重排(build→bind→swap→teardown);`ready_tx` 接线;全局 registry 安装时机移至 swap 后;同端口 handoff 策略(SO_REUSEADDR 或 bind-handoff)。
- 禁区:diff 增量重启留二期;不动 V2Ray 复用已闭环逻辑;router/no-router 两份实现必须同步改。
- 前置:建议先做 probe(H-9 Go reload 语义对照 + H-2 GUI reload 使用方式)。**必须串行于 PKG-INBOUND-LIVENESS-01 之前或合并设计**(ready 信号是共享基础设施)。
- 验收:RD-01 描述的测试(activation-fail 后旧端口仍 accept);bind-fail 导致 reload 报 Failed;回滚后 detour 拨号命中旧 bridge 注册表;rollback_guard 9+1 全保持 PASS。
- 风险:**高**(核心路径重排);需真实 listener 测试矩阵护航,分小步提交。

**PKG-INBOUND-LIVENESS-01** — inbound 数据面可观测性
- 目标:inbound serve 任务有 monitor(panic/error 分类 + metrics);启动 ready 与 bind 解耦修复。覆盖:CAL-06、CAL-16(DNS 静默)、CAL-13(API-down 信号,policy 允许范围内)。
- 前置:与 PKG-RELOAD-CONTINUITY-01 共享 ready/monitor 基础设施,建议紧随其后。
- 验收:注入 panic 的 inbound → 进程日志/metrics 可见;resolver 构建失败有 warn。
- 风险:中。

**PKG-TUN-DATAPLANE-01** — TUN 数据面可用化
- 目标:GUI stack 枚举(system/gvisor/mixed)映射到真实栈;Enhanced(smoltcp)路径真实流量验证;Manual no-op 路径明确处置(实现或显式拒绝)。覆盖:CAL-10、未知项 #5。
- 前置:PKG-SCHEMA-PARITY-01(否则配置进不来)。
- 验收:macOS 实机 TUN 起 → curl 经隧道出站成功;GUI 默认 stack 值可启动。
- 风险:**高**(数据面 + 权限 + 平台);可能暴露 smoltcp 栈的深层问题 → 任务包内置"评估后升级/降级"决策点。

**PKG-GUI-E2E-01** — GUI 实机端到端验收 harness(验证包,不改产品代码)
- 目标:真 GUI.for.SingBox 拉起 Rust 内核走通主流程,产出逐步骤三态记录;消除未知项 #1/#2/#3。
- 前置:PKG-GUI-CONTRACT-01 + PKG-SCHEMA-PARITY-01。
- 验收:启动识别/选节点/系统代理模式可上网三步 PASS(TUN 步待 PKG-TUN-DATAPLANE-01);发现的新断裂登记新卡不顺手修。
- 风险:低(纯验证)。

**PKG-LINT-GATE-01** — 安全 lint 护栏决策(POLICY)
- 目标:决策 `lints.workspace=true` 推广范围与存量 unwrap 豁免策略(CAL-08)。先做影响面报告(每 crate 违规计数),交用户拍板,再分批落地。
- 禁区:不一次性全仓开闸。验收:决策记录 + 首批 crate 通过。风险:低(纯增量门禁)。

**PKG-HYGIENE-01** — 运行时与测试卫生清扫
- 覆盖:CAL-11(eprintln→tracing)、CAL-27(2 warn)、CAL-19(selector 测试重写或 de-scope)、CAL-28(trojan ignore 测试上 harness)、CAL-29(flake 加固)、CAL-24(心跳 task cancel)。
- 验收:逐项绿灯;无行为变化(纯卫生)。可并行:任何时候。风险:低。

**PKG-DOC-CALIBRATE-01** — 外部文档校准
- 覆盖:CAL-26 簇(capabilities.json 重生成、README 数字降级/加锚、TEST_EXECUTION_SUMMARY/CLEANUP_REPORT 归档)+ 本报告结论回写 active_context(种子 01A 升级、新 P0 登记)。
- 验收:外部文档与 D 节校准一致;active_context ≤100 行纪律保持。风险:低。

---

## J. 推荐的下一步

1. **首选:PKG-GUI-CONTRACT-01 + PKG-SCHEMA-PARITY-01 并行启动**(两者无依赖、改动面小、都是 P0、合计解锁 GUI 验证面)。
2. **为什么优先**:三个 P0 中这两个成本最低且互不耦合;PKG-WG-DATAPLANE-01(第三个 P0)虽也独立,但 WG 不在 GUI 最短主流程上,可紧随其后。最高目标的最短路径是"GUI 能拉起 + 配置能进来",其它一切验证都堵在这两道门后。
3. **若被 probe 推翻的替代**:若 H-2 probe 发现 GUI 对启动确认有超时兜底(字符串非硬阻塞),则 CAL-02 降级,首选改为 PKG-SCHEMA-PARITY-01 + PKG-WG-DATAPLANE-01;若 H-4 扫描发现 schema 缺口远超 TUN,则 PKG-SCHEMA-PARITY-01 扩容并独占优先。
4. **可并行**:PKG-GUI-CONTRACT-01 ∥ PKG-SCHEMA-PARITY-01 ∥ PKG-HYGIENE-01 ∥ PKG-DOC-CALIBRATE-01(互不触碰);PKG-WG-DATAPLANE-01 可随时插入。
5. **必须推迟/串行**:PKG-RELOAD-CONTINUITY-01(高风险核心重排,先做 H-2/H-9 probe 定设计锚点,且建议在 GUI 验证面打开后再动,以便用 PKG-GUI-E2E-01 做回归);PKG-INBOUND-LIVENESS-01 紧随其后(共享 ready/monitor 基建);PKG-TUN-DATAPLANE-01 在 schema 包落地后;PKG-LINT-GATE-01 等用户 POLICY 拍板。

**只提出建议,不自动领取任何任务包。**

---

## K. 最终仓库状态

```
git status --short --branch:
## main...origin/main
?? agents-only/a0_reality_spike/
?? agents-only/fable5审计报告/        ← 用户显式授权的审计产物(未提交)

git rev-parse HEAD:        02d8d16e5061e4d7fb60bf30e2899d2099000acf
git rev-parse origin/main: 02d8d16e5061e4d7fb60bf30e2899d2099000acf
```

确认:
- audit-only 阶段**零 tracked 修改**(全程只读 + 仅授权目录新建);
- **没有新增 commit**(HEAD 不变);
- **没有额外 push**(远端不变);
- `agents-only/a0_reality_spike/` **未触碰**;
- 唯一新增 = 本目录(`agents-only/fable5审计报告/`,用户 2026-06-10 显式授权;是否提交由用户决定)。
