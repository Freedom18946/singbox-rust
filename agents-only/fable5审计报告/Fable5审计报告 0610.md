### Fable5审计报告 0610

REPO-GLOBAL-CALIBRATION-01A 审计报告

---
A. Push 结果

┌────────────────────────────────────────────────────────┬──────────────────────────────────────────────────────────┐
│                           项                           │                            值                            │
├────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────┤
│ push 前 HEAD                                           │ 02d8d16e                                                 │
├────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────┤
│ push 前 origin/main(本地 ref + git ls-remote 远端实测) │ 02d8d16e                                                 │
├────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────┤
│ push 的 commit 范围                                    │ 无 —— 偏差:push 已在先前会话完成                         │
├────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────┤
│ push 输出                                              │ 未执行(条件不成立:本地 ahead 0,非预期 ahead 2)           │
├────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────┤
│ push 后 HEAD / origin/main                             │ 02d8d16e / 02d8d16e(已达成任务书的最终预期状态)          │
├────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────┤
│ 最终 git status --short --branch                       │ ## main...origin/main + ?? agents-only/a0_reality_spike/ │
├────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────┤
│ a0_reality_spike/                                      │ 全程未触碰(仅 ls 只读确认存在)                           │
└────────────────────────────────────────────────────────┴──────────────────────────────────────────────────────────┘

偏差说明:任务书预期"ahead 2、origin/main=907f5944",实际 git fetch + git ls-remote origin main 双重确认远端已在 02d8d16e。两个 commit(47e15b0b+02d8d16e)已在远端。按任务书纪律未执行任何 push/reset/rebase,直接进入 audit-only。

---

B. 审计范围地图

┌────────────────────────────────────────────────────────────────────┬────────────────────────────────────────────────┬──────────────────────────┐
│                                区域                                │                     覆盖度                     │           方式           │
├────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────┼──────────────────────────┤
│ app/src/(bootstrap/run_engine/sidecar/inbound_starter/bin)         │ 完整                                           │ R-B1/R-C 全读 +          │
│                                                                    │                                                │ 主线程复核               │
├────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────┼──────────────────────────┤
│ crates/sb-core/runtime/supervisor.rs(2734 行)                      │ 完整                                           │ R-B1/R-C 双盲交叉 +      │
│                                                                    │                                                │ 主线程逐行复核关键段     │
├────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────┼──────────────────────────┤
│ crates/sb-core/(context/adapter/bridge/registry/services/endpoint) │ 完整(核心面)                                   │ R-B1/R-B2                │
├────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────┼──────────────────────────┤
│ crates/sb-adapters/(register.rs + inbound/outbound)                │ 完整(注册面)+ 抽样(协议内部)                   │ R-A/R-B1/R-B2            │
├────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────┼──────────────────────────┤
│ crates/sb-config/(validator/v2 + ir)                               │ 完整(schema 面)                                │ R-A/R-B2 + 主线程复核    │
├────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────┼──────────────────────────┤
│ 其余 13 个 crates                                                  │ 抽样(危险模式 grep 全量 + 人工判读)            │ R-B2                     │
├────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────┼──────────────────────────┤
│ 测试(~4307 个测试函数,157 #[ignore])                               │ 抽样分类 + 关键套件实跑                        │ R-D                      │
├────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────┼──────────────────────────┤
│ 门禁(boundaries/clippy/rollback_guard)                             │ 实跑验证                                       │ R-D                      │
├────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────┼──────────────────────────┤
│ 文档(docs/ + agents-only/ + README + reports/)                     │ 完整(权威链)+ 抽样(docs 根 45+ 文件)           │ 主线程 + R-D             │
├────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────┼──────────────────────────┤
│ GUI_fork_source/(GUI 真实依赖)                                     │ 针对性(kernel 拉起契约 + TUN profile 生成)     │ R-A + 主线程复核         │
├────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────┼──────────────────────────┤
│ go_fork_source/(Go 对照)                                           │ 针对性(box.go 启动输出)                        │ R-A                      │
├────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────┼──────────────────────────┤
│                                                                    │ sb-core 1109                                   │                          │
│ 未能确认                                                           │ 测试全量未跑(抽样绿);Windows/Linux             │ —                        │
│                                                                    │ 平台路径未实机;真实 GUI 进程未实际拉起         │                          │
└────────────────────────────────────────────────────────────────────┴────────────────────────────────────────────────┴──────────────────────────┘

实测构建:R-A 完成 cargo build -p app --features parity 并对二进制做了 strings/运行实测 —— 多个发现是运行级证据,非静态推断。

---

C. 权威文档与最高目标

文档优先级(经交叉验证):
1. agents-only/active_context.md — 高度可信,R-D 抽验全部吻合(commit 引用、flake 登记、续接缺口登记)
2. labs/interop-lab/docs/dual_kernel_golden_spec.md — 行为对齐权威
3. agents-only/reference/GO_PARITY_MATRIX.md — 可信且诚实标注陈旧锚点(209/209 = 验收口径 ≠ 行为对齐 183/209)
4. docs/STATUS.md / docs/capabilities.md — 对冲得当,但 reports/capabilities.json 落后 HEAD 335 commits
5. 已漂移:docs/TEST_EXECUTION_SUMMARY.md(严重误导)、docs/CLEANUP_COMPLETION_REPORT.md、README 头牌数字(149x/36/38 无锚点)

最高目标(≤200 字):交付一个可直接替换 Go sing-box 1.13.13 的 Rust 二进制,使 GUI.for SingBox 1.25.1 无感知地把它当 sing-box 使用。核心用户价值:GUI 用户的全部主流程(拉起内核、TUN/代理模式、节点选择、热重载)在 Rust 内核上行为一致且更可靠。当前最近里程碑:GUI 实拉起闭环。真正 DoD:GUI 用默认 profile(TUN 模式)拉起 Rust 二进制 → 收到启动确认 → 数据面通 → reload 不致服务中断。不是"代码存在 + 测试绿 + 矩阵 closed"。

文档矛盾点:README 称 "Maintenance mode" 与用户最高目标(记忆已校正)矛盾;parity"209/209 closed"语言与三个 P0 级 GUI 断裂并存——验收口径掩盖了产品路径断层。

---

D. 全局完成度校准

┌──────────────────────────────┬──────────────────┬───────────────────────┬───────────────┬───────────────────────────────┬────────────┬────────┐
│         子系统/能力          │ 对最高目标的作用 │       实现证据        │   测试证据    │           当前缺口            │ 完成度区间 │ 置信度 │
├──────────────────────────────┼──────────────────┼───────────────────────┼───────────────┼───────────────────────────────┼────────────┼────────┤
│                              │                  │ server.rs 全端点 +    │ A 类(真       │                               │            │        │
│ clash_api 管理面             │ GUI 操作核心     │ 实测 PUT/delay/WS     │ axum+reqwest  │ API-down 无机器信号           │ 85%–95%    │ 高     │
│                              │                  │                       │ e2e)          │                               │            │        │
├──────────────────────────────┼──────────────────┼───────────────────────┼───────────────┼───────────────────────────────┼────────────┼────────┤
│                              │                  │ parity 二进制实测     │ A/B 混合(最深 │                               │            │        │
│ 主流协议数据面(TCP)          │ 代理本体         │ vmess 链路            │  trojan 测试  │ 长尾协议 stub                 │ 80%–90%    │ 高     │
│                              │                  │                       │ ignore)       │                               │            │        │
├──────────────────────────────┼──────────────────┼───────────────────────┼───────────────┼───────────────────────────────┼────────────┼────────┤
│ 配置 schema 兼容             │ GUI 配置可吃     │ golden roundtrip 测试 │ A 类          │ TUN 现代字段被拒(P0)          │ 55%–70%    │ 高     │
├──────────────────────────────┼──────────────────┼───────────────────────┼───────────────┼───────────────────────────────┼────────────┼────────┤
│ GUI 进程契约                 │ 拉起闭环入口     │ 启动字符串/版本号实测 │ 无            │ 'sing-box started' 缺失(P0)   │ 50%–65%    │ 高     │
├──────────────────────────────┼──────────────────┼───────────────────────┼───────────────┼───────────────────────────────┼────────────┼────────┤
│ TUN 数据面                   │ GUI 默认模式     │ tun/mod.rs 自标       │ 部分(smoltcp  │ Manual=no-op;GUI stack        │ 15%–30%    │ 高     │
│                              │                  │ skeleton              │ 路径)         │ 枚举不识别                    │            │        │
├──────────────────────────────┼──────────────────┼───────────────────────┼───────────────┼───────────────────────────────┼────────────┼────────┤
│ WireGuard(endpoint+outbound) │ 1.12 主推形态    │ 实测硬启动失败        │ 无            │ endpoint 孤岛 + outbound 恒   │ 10%–25%    │ 高     │
│                              │                  │                       │               │ stub                          │            │        │
├──────────────────────────────┼──────────────────┼───────────────────────┼───────────────┼───────────────────────────────┼────────────┼────────┤
│ reload/rollback              │ 可靠性面         │ 01B/01C 闭环 + 10     │ A 类(真       │ continuity/readiness/registry │ 60%–75%    │ 高     │
│                              │                  │ 测试实跑 PASS         │ listener)     │  三缺口(P1 群)                │            │        │
├──────────────────────────────┼──────────────────┼───────────────────────┼───────────────┼───────────────────────────────┼────────────┼────────┤
│ DNS                          │ 数据面支撑       │ 全传输 from_spec +    │ A/B 混合      │ 坏 resolver 静默跳过          │ 75%–85%    │ 中高   │
│                              │                  │ 实测启动              │               │                               │            │        │
├──────────────────────────────┼──────────────────┼───────────────────────┼───────────────┼───────────────────────────────┼────────────┼────────┤
│ 路由引擎                     │ 数据面支撑       │ SRS 二进制解析对齐 Go │ A 类(CLI e2e) │ 未逐规则验证                  │ 75%–85%    │ 中高   │
├──────────────────────────────┼──────────────────┼───────────────────────┼───────────────┼───────────────────────────────┼────────────┼────────┤
│ 可观测性(liveness)           │ 运维可靠性       │ sidecar               │ 部分          │ inbound panic=静默死端口      │ 40%–55%    │ 高     │
│                              │                  │ 有监控、inbound 无    │               │                               │            │        │
└──────────────────────────────┴──────────────────┴───────────────────────┴───────────────┴───────────────────────────────┴────────────┴────────┘

全仓面向最高目标的加权完成度区间:60%–70%
置信度:中高
判断依据:管理面与主流协议数据面真实完成度高(实测级证据);但 GUI 默认
路径上存在 3 个 CONFIRMED P0 断裂(TUN schema、启动字符串、WG endpoint),
reload 可靠性存在 P1 缺口群。其中 2 个 P0 是薄修复(字符串+schema 字段),
1 个 P0 是接线工程(WG);TUN 数据面是最大的真实工程缺口。
加权逻辑:GUI 拉起闭环(契约+schema)与数据面入口(TUN/WG)按阻塞性
加重;管理面高完成度无法补偿入口断裂。

---

E. 已确认完成的关键能力(全部有实测/代码证据)

1. pre-swap rollback 事务(47e15b0b):单 async 块收口全部 manager 阶段失败 → shutdown_failed_reload_context;fresh/inherited V2Ray 指针判别正确;10 个 rollback_guard 测试实跑 PASS(9+1,4 个 feature-gated)
2. V2Ray 跨 reload 复用(7dc853ef):严格门控(双 enabled+结构等+真 Running),所有权语义无漏洞
3. 重复失败 reload 无泄漏:旧 context 稳定存活,每次失败只清自己的新资源(测试 e_ 验证)
4. shutdown/reload 串行化:单 mpsc 事件环 + state.write 内变更,无双消费者
5. clash_api 全套 GUI 端点 + 节点选择 sled 持久化:实测 204/生效/落盘
6. 协议解析 panic 安全:trojan/vless/vmess/ss/socks 网络数据路径零可触发 panic(2493 unwrap 逐类排查,全为 mutex/不变式)
7. 坏配置不致崩溃且不触发 step0:validate 失败在 supervisor.reload 之前拦截,运行态完好
8. 门禁声明属实:boundaries exit 0(537 断言)、clippy all-features 0 warn —— 实跑验证
9. SRS 二进制 rule-set / DNS 全传输 / 进程匹配四平台 loud-fail 真实现

---

F. 已确认缺陷与明显缺口

P0 — 阻塞 GUI drop-in 主流程

[CAL-01] GUI 默认 TUN 配置被 schema 拒绝,内核无法启动
类型:MISSING FEATURE(schema)| 状态:CONFIRMED(主线程复核)
证据:crates/sb-config/src/validator/v2/inbound.rs:8-34(allowed_inbound_keys 无 mtu/address/stack/strict_route/route_address);crates/sb-config/src/ir/inbound.rs:449(InboundIR 无这些字段);GUI 生成器 GUI_fork_source/.../utils/generator.ts:81-90;crates/sb-config/src/lib.rs:520-537 无条件严格校验。R-A 实测复现拒绝。
触发:GUI 默认 profile(TUN 模式)| 影响:run 直接报 unknown field,GUI 主模式完全无法启动
最小验证:app run -c <含 mtu/address/stack 的 tun 配置>

[CAL-02] 启动确认字符串缺失,GUI 判定内核启动失败(影响所有模式)
类型:SEMANTIC GAP(进程契约)| 状态:CONFIRMED(主线程复核)
证据:GUI constant/kernel.ts:17 CoreStopOutputKeyword='sing-box started' + kernelApi.ts:257 out.includes(...);Rust app/src/run_engine_runtime/output.rs:102/107 输出 singbox-rust booted/started pid=,均不含该子串;Go 对照 box.go:434。
触发:GUI 拉起内核(任何模式)| 影响:GUI 永不收到启动确认 → 超时判定失败,即使内核实际在跑
最小验证:app run … 2>&1 | grep 'sing-box started'(零命中)

[CAL-03] WireGuard endpoint 构建后不进 outbound 命名空间 → 路由引用即硬启动失败
类型:SEMANTIC GAP(孤岛接线)| 状态:CONFIRMED(主线程复核)
证据:crates/sb-core/src/adapter/bridge.rs:838-855(Step 4 仅 add_endpoint + endpoints_map 供 services);crates/sb-core/src/endpoint/mod.rs:588-624(as_outbound_connector/EndpointAsOutbound 零生产调用点,主线程 grep 复核)
触发:endpoints:[wireguard] + route 指向其 tag | 影响:实测 default outbound not found 硬失败;1.12 现代 WG 形态在 run 路径不可用
最小验证:R-A 已实测复现

P1 — 阻塞可靠性面 / drop-in 行为

[CAL-04] reload 失败 → 旧 inbound 已死且永不恢复(服务中断,种子 01A 实锤升级)
类型:BUG(可用性)| 状态:CONFIRMED(三 agent 独立一致 + 主线程复核 supervisor.rs:572-597)
证据:step0 在 Engine::from_ir(:600,可失败)之前 request_shutdown 全部旧 inbound + 1200ms grace;失败后 run loop 只 log Failed 继续(run_engine_runtime/supervisor.rs:355-367),无恢复路径。Go 行为:先建后拆,坏 reload = no-op。
触发:配置过 validate 但 activation 失败(outbound build 错/Start 错)| 影响:GUI 用户一次坏 apply = 全端口断流直到下次成功 reload
最小验证:SIGHUP 一个 activation 必败配置,观察旧端口停 accept 且不恢复

[CAL-05] inbound bind 失败不可见、不致命:startup 报 started,reload 照常 swap
类型:BUG(正确性+可观测)| 状态:CONFIRMED(主线程复核 supervisor.rs:657-664)
证据:spawn_blocking(ib.serve()) JoinHandle 丢弃,serve Err 仅 tracing::error!,不进 activation_result;ready_tx 全线传 None
触发:端口被占/EACCES | 影响:进程报"成功"但端口无人监听;叠加 CAL-04 = 成功报告掩盖永久服务空洞
最小验证:占用 1080 后启动 mixed inbound,观察 started 照报

[CAL-06] inbound accept loop 无 liveness 监控/watchdog/重启(对照:sidecar 有完整监控)
类型:ARCHITECTURE DEBT | 状态:CONFIRMED
证据:supervisor.rs:228/402/659/829 JoinHandle 全丢弃;对照 admin_start.rs:277-302 Clash 有 classify_exit
触发:serve task panic/异常返回 | 影响:主数据面是全系统可观测性最弱组件——端口静默关闭,进程看似健康
最小验证:在 serve_mixed 注入 panic,观察进程存活+端口死+无信号

[CAL-07] 全局 RUNTIME_OUTBOUNDS/INBOUNDS 在 pre-swap 即被新 bridge 覆盖,回滚不恢复
类型:BUG(reload 原子性)| 状态:CONFIRMED(主线程复核)
证据:bridge.rs:768/836(build_bridge 内安装全局,reload build 在 swap 前);shutdown_failed_reload_context(supervisor.rs:1487-1500)不碰 registry;detour.rs:23 连接热路径读该全局
触发:reload 失败回滚后任何 detour 拨号 | 影响:detour 解析到已被丢弃的 bridge 注册表 → 错实例/拨号失败;不在 01A/01B/01C 任何已知卡覆盖范围内,系本轮新发现
最小验证:含 detour 配置 + Start 阶段必败 reload,回滚后断言 runtime_outbounds() 非旧 bridge

[CAL-08] reload 无 diff:全量重启所有 inbound,违背自身文档与 Go 增量语义
类型:SEMANTIC GAP | 状态:CONFIRMED
证据:supervisor.rs:12 文档承诺 diff/不断连;实现无任何 diff 分支(:572-577 无条件全关);diff 仅用于日志
触发:任何配置变更 reload | 影响:改一条规则 = 全端口断连窗口
最小验证:双 inbound 改无关 outbound,SIGHUP,双端口均中断

[CAL-09] wireguard outbound(legacy)在 parity/full feature 下恒为 stub
类型:MISSING FEATURE(feature 接线)| 状态:CONFIRMED
证据:真实现门 adapter-wireguard-outbound(register.rs:2771),但无任何 app feature 启用它(sb-adapters/Cargo.toml:82-83);parity 二进制 strings 实测无 wireguard outbound
最小验证:strings target/.../run | grep outbound/wireguard(0)

[CAL-10] TUN 数据面:默认 Manual=skeleton no-op;GUI 的 stack 识别
类型:MISSING FEATURE | 状态:CONFIRMED
证据:crates/sb-adapters/src/inbound/tun/mod.rs:8(自标 skeleckend:mixed/gvisor → InvalidInput)、:468-517/:526(Manualno-op、macOS 丢弃负载);唯一真实路径 stack:"smoltcp",GUI 无此选项
影响:即使 CAL-01 修复,GUI TUN 数据面仍不通 —— 这是最大的真

[CAL-11] workspace 安全 lint(deny unwrap/expect/panic)对 15
类型:ARCHITECTURE DEBT(流程护栏)| 状态:CONFIRMED(主线程脚本复核:仅 sb-tls 继承)
证据:根 Cargo.toml:47-50 定义 deny;[lints] workspace=true
影响:当前健壮性(E.6)靠人工纪律,无机器强制,随时间漂移风险;补齐是 POLICY 决策(会暴露 2493 处存量 unwrap 需豁免评估)



P2(精选)

- [CAL-12] request_shutdown 信号丢失竞态:stop_tx 注册晚于 spawn_blocking 调度,信号 take() 到 None 静默丢弃 → 端口悬挂/EADDRINUSE(register.rs:3705-3728)| HIGH-CONF INFERENCE
- [CAL-13] Clash API down 仅一条 error log,无机器可读信号;crash 事件 bridge 仅 debug 级(admin_start.rs:341-411;RC-05/13)| CONFIRMED
- [CAL-14] 无 SO_REUSEADDR + 1200ms grace 是时序启发式而非释放屏障(mixed.rs:58 等)| CONFIRMED
- [CAL-15] V2Ray 快速 disable→enable 同端口:close() 异步释放 → 瞬时 EADDRINUSE → warn+skip,API 静默缺席 | HIGH-CONF INFERENCE
- [CAL-16] 坏 DNS resolver 静默跳过(if let Ok 无 else,supervisor.rs:204-208/648-652)→ 用户配置的 DNS 静默不生效 | CONFIRMED
- [CAL-17] 生产路径残留 eprintln!(register.rs:3724、http.rs:308、tuic.rs:847、hysteria2.rs:544,连接热路径)| CONFIRMED
- [CAL-18] graceful shutdown 的 drain 形同虚设:serve 返回即嵌套 runtime drop,per-conn task 被 abort(register.rs:3710-3720)| HIGH-CONF INFERENCE
- [CAL-19] 版本号 0.1.0 可能触发 GUI 版本门控降级 | OPEN QUESTION(需 probe GUI 比较逻辑)
- [CAL-20] selector/proxy-pool 4 测试 ignore 腐烂 ~7.5 月,行为域覆盖静默削弱(RD-06)| CONFIRMED

P3(汇总)

文档漂移群(capabilities.json 落后 335 commits、TEST_EXECUTION_SUMMARY 严重误导、CLEANUP_REPORT 自相矛盾、README 头牌数字无锚点);FakeIP 掩码/experimental 块解析静默 .ok()(RB2-03/04);system_proxy 小众平台假成功(RB2-05);serve_http 心跳 task 共享 runtime 路径泄漏(RB1-06,生产无调用方);特定 feature 下 cargo test 2 warn(RD-07);trojan 最深握手测试 ignore(RD-09)。

---

G. 被推翻或需降级的旧判断

┌────────────────────────────────────────────────┬───────────────────────────────────────────────────────────────────────────────────────────────┐
│                     旧判断                     │                                             裁定                                              │
├────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────┤
│ ServiceManager::close() no-op →                │ DISPROVED(推论):全部 8 处关闭路径有显式 stop_services 补偿。no-op                             │
│ 隐含"会漏关服务"                               │ 事实成立但当前无泄漏;保留为结构债(手工对齐非结构保证)                                         │
├────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────┤
│ APP-RELOAD-INBOUND-CONTINUITY-01A = DEFER      │ ↑ 升级 P1 活动卡:三个 agent 独立确认被低估——真实影响 = CAL-04+05+08+12 合集,不只是"提前关"    │
├────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────┤
│ SVC-V2RAY-API-01B = DEFER/POLICY               │ 维持:无行为缺陷,reuse/rollback 实现正确;仅 CAL-15 边角                                        │
├────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────┤
│ APP-SIDECAR-POLICY-02A = DEFER/FUTURE          │ 维持但附挂 CAL-13:liveness 政策自洽;开口是 ownership+观测信号                                 │
├────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────┤
│ TIDY-RUSTDOC-LINKS / TIDY-APP-BREAKER-FLAKE    │ 维持低优:纯卫生;breaker flake 隔离即过                                                        │
├────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────┤
│ "维护态接近完成 / 209/209 closed" 的整体印象   │ 需校准:管理面真完成,但 GUI 入口(TUN schema、启动契约、WG)存在结构性断裂,不被                  │
│                                                │ accepted-limitation 覆盖                                                                      │
├────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────┤
│ bootstrap.rs::start_from_config                │ DISPROVED:零生产调用方,legacy(lib.rs 自注)                                                    │
│ 是活路径的隐含假设                             │                                                                                               │
├────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────┤
│ outbound/registry GLOBAL OnceCell 是 reload    │ DISPROVED:env-only 驱动,reload 不触                                                           │
│ 缺陷的怀疑                                     │                                                                                               │
├────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────┤
│ 应合并                                         │ CAL-04/05/08/12(reload 连续性群)应作为一个 coherent 任务包,不拆碎卡                           │
└────────────────────────────────────────────────┴───────────────────────────────────────────────────────────────────────────────────────────────┘

---
H. 最危险的未知项(≤10)

1. GUI 版本门控对 0.1.0 的具体行为 — 可能整体降级字段集;probe:读 GUI getKernelVersion 比较分支 + 实拉起观察
2. 真实 GUI 进程从未实际拉起过 Rust 内核 — 所有 GUI 兼容结论来自源码对照;probe:本机跑 GUI.for SingBox 指向 Rust 二进制(修 CAL-02 后)
3. smoltcp TUN 路径的真实数据面质量 — EnhancedTunInbound 自标 skeleton 但有 connect/relay 测试;probe:smoltcp stack 实流量 e2e
4. router/no-router 双份 handle_reload 的漂移风险 — 当前一致但手工同步;probe:结构 diff 两函数
5. 1200ms grace 在真实负载下是否够 — 时序启发式;probe:负载下重复同端口 reload
6. sb-core 1109 测试全量从未在本轮跑过(抽样绿);probe:全量跑一次
7. Go 配置 schema 兼容面只抽样了 GUI 风格 — 其他 Go 1.12 字段族(如 multiplex、transport 变体)可能同样被严格 schema 拒;probe:用 Go 仓 test 配置集喂 app check
8. Windows/Linux 平台路径零实机验证(本轮 macOS)
9. REALITY official-JA4 + camouflage 仍 OPEN(已知,封箱口径正确,列此为完整性)
10. CAL-07 的实际爆炸半径 — detour 使用率决定严重度;probe:统计 detour 在典型 GUI 配置的出现率

---

I. 建议任务包(8 个)

TP-01 GUI-CONTRACT-01:GUI 进程契约对齐
目标:启动输出含 sing-box started(对齐 box.go:434 格式)+ 版本号策略决策(报 sing-box 兼容版本 vs 0.1.0)
覆盖:CAL-02、CAL-19 | 为什么现在:最小努力解锁所有模式的 GUI 拉起确认
范围边界:仅 output.rs + version 表面;禁区:不动日志架构
前置:无 | 验收:run 输出 grep 命中;GUI 实拉起收到确认(连 H-2 probe)
门禁:全量 test + boundaries | 风险:低

TP-02 TUN-SCHEMA-01:TUN 配置 schema 现代化
目标:InboundIR + allowed_inbound_keys 补 address/mtu/stack/strict_route/route_address/route_exclude_address/endpoint_independent_nat 等 Go 1.12 字段;stack 枚举接受 system/gvisor/mixed(语义映射决策:映射到现有 backend 还是显式 unsupported 报错)
覆盖:CAL-01 + CAL-10 的 schema 面 | 为什么现在:GUI 默认 profile 的解析门槛
禁区:不在本包实现 TUN 数据面 | 前置:无 | 可与 TP-01 并行
验收:GUI 生成的 TUN 配置 app check 通过 | 风险:字段语义映射错误 → 用 golden 对照 Go

TP-03 RELOAD-CONTINUITY-01:reload 连续性与原子性重构(吸收种子 01A,升级)
目标:① build-before-teardown 顺序(新构建成功后才动旧 inbound);② inbound bind readiness 纳入激活事务(启用 ready_tx 通道);③ 回滚恢复全局 registry;④ 失败路径旧 inbound 恢复保证
覆盖:CAL-04、CAL-05、CAL-07、CAL-12、CAL-14(部分) | 为什么现在:可靠性面唯一 P1 群,Go 行为偏差核心
范围边界:supervisor.rs 双路径(router/no-router 同步改);禁区:不顺手做 diff 增量(那是 TP-04)
前置:无 | 验收:坏 reload = no-op(旧端口持续 accept);bind 失败 reload 报 Failed 不 swap;回滚后 detour 拨号正确
门禁:rollback_guard 全套 + 新增 continuity 测试(真 listener)| 风险:中高(swap 时序重排;同端口新旧并存需 SO_REUSEADDR 或换 bind 策略)

TP-04 RELOAD-DIFF-01:inbound 增量 diff 重启
目标:只重启变化的 inbound,对齐 supervisor.rs:12 自身承诺与 Go 语义
覆盖:CAL-08 | 前置:TP-03(连续性语义先立) | 验收:改无关项 reload,无关端口零中断
风险:diff 等价判定;可后置

TP-05 INBOUND-LIVENESS-01:inbound 存活监控
目标:保留 serve JoinHandle + panic/异常退出分类(复用 admin_start.rs classify 模式)+ 可观测信号(log error + metrics/状态端点)
覆盖:CAL-06,连带 CAL-13(sidecar 状态信号统一)、CAL-16(DNS 静默跳过补 warn)
前置:与 TP-03 有接口耦合(ready/handle 管道),建议紧随其后 | 验收:注入 panic → error 级信号 + 状态可查
风险:低中

TP-06 WG-WIRING-01:WireGuard 接线
目标:① endpoint 注册进 outbound 命名空间(已有 EndpointAsOutbound,纯接线);② adapter-wireguard-outbound feature 接入 parity 集或显式 de-scope 决策
覆盖:CAL-03、CAL-09 | 可与 TP-03 并行(不同文件域)
验收:endpoints:[wireguard] + route 引用启动成功并可拨号 | 风险:boringtun 数据面质量是后续问题,本包只管接线

TP-07 HYGIENE-SWEEP-01:生产卫生清理
目标:eprintln 清除(CAL-17)+ feature 组合 2 warn(RD-07)+ FakeIP/experimental 静默 .ok() 补 issue(RB2-03/04)+ selector 测试腐烂处置(CAL-20)+ trojan ignore 重启用评估(RD-09)
全部 P2/P3 卫生,可并行任意时点 | 风险:低

TP-08 DOC-CALIBRATION-01:文档与台账校准
目标:capabilities.json 重生成 + TEST_EXECUTION_SUMMARY/CLEANUP_REPORT 归档 + README 头牌数字加锚点或降级 + 本审计结论落入 active_context(种子重分级)
覆盖:RD-02..05 + G 节裁定 | 验收:权威链零自相矛盾 | 风险:零

lint 护栏(CAL-11)单列为 POLICY 决策项,不成包:需用户决断是否推广 lints.workspace = true(会暴露 2493 存量 unwrap 的豁免工程)。

---
J. 推荐的下一步

1. 首选:TP-01 GUI-CONTRACT-01 —— 它优先因为:(a) 努力最小(output 字符串 + 版本表面),(b) 解锁 H-2 这个最危险未知项的 probe(真实 GUI 实拉起),(c) 是后续一切 GUI 闭环验证的前提观测通道。
2. 若 TP-01 经 probe 被推翻(比如发现 GUI 实际用其他机制判定启动):替代首选 TP-03 RELOAD-CONTINUITY-01(独立于 GUI 契约的最大可靠性缺口,证据已三方确认)。
3. 可并行:TP-01 ∥ TP-02 ∥ TP-06(文件域不相交);TP-07/TP-08 随时插入。
4. 必须推迟:TP-04(依赖 TP-03 语义);TUN 数据面真实化(CAL-10 全量,最大工程,等 TP-02 schema 通过 + GUI 实拉起验证后再立项排量);lint 护栏推广(等 POLICY 决断)。

只建议,不领取。

---
K. 最终仓库状态

$ git status --short --branch
## main...origin/main
?? agents-only/a0_reality_spike/

$ git rev-parse HEAD
02d8d16e5061e4d7fb60bf30e2899d2099000acf

$ git rev-parse origin/main
02d8d16e5061e4d7fb60bf30e2899d2099000acf

确认:✅ audit-only 阶段零 tracked 修改;✅ 无新增 commit(HEAD 仍 02d8d16e);✅ 无额外 push;✅ agents-only/a0_reality_spike/ 未触碰(仅只读 ls)。临时产物仅在 /tmp。

---
审计总结一句话:rollback/泄漏面(最近三轮工作)是真实闭环且测试扎实的;但站在最高目标看,GUI 入口存在三个未登记的 P0 断裂(TUN schema、启动契约、WG 孤岛),reload 连续性是被系统性低估的 P1 群,且本轮新发现了不在任何已知卡内的全局 registry 回滚缺口(CAL-07)——"验收 209/209"的口径掩盖了产品路径上的这些断层。

完成回报,停止。



REPO-GLOBAL-CALIBRATION-01A 全部完成:push 偏差已核实记录(先前会话已完成),audit-only 全仓审计已交付 A–K 报告,仓库零变更。等待下一步命令。







压缩精简：完整无压缩版已落盘 agents-only/fable5审计报告/(7 个文件:5 份 agent 原文 + 06_全局汇报_A-K.md 主报告 + README 索引,未提交 git)。以下为结构化摘要:

---
A. Push 结果

偏差:任务书预期 ahead 2 / origin=907f5944;实际核验 + git ls-remote 确认 push 已在先前会话完成,远端真值 = 02d8d16e。按已批准计划:记录偏差、跳过 push(目标状态已达成)。a0_reality_spike/ 全程未触碰。

B. 审计范围

完整扫描:app 运行时面、supervisor.rs 全 2734 行、sb-core 生命周期面、register.rs 注册分支、GUI 契约面(kernel.ts/generator.ts)、agents-only 文档。抽样:协议实现细节、~4307 测试函数、其余 crates。实跑验证:boundaries(exit 0/537 断言)、clippy all-features(0 warn)、rollback_guard(9+1 PASS)。未确认:GUI 实机、Windows/Linux、字节级协议对齐。

C. 最高目标

可直接替换 Go sing-box 1.13.13 的 Rust 二进制 + GUI.for SingBox 完全兼容。关键文档冲突:MT-GUI-04 "55/55" 与两个 GUI P0 并存 ⇒ 该验收从未含真 GUI 进程拉起路径;supervisor.rs:12 的 "Hot Reload 不断连接" 承诺与实现直接矛盾。

D. 加权完成度

全仓面向最高目标加权完成度:65%–75%(置信度中高)
管理面/主流协议(85%+)拉高基线;但 GUI 必经的三道门(schema/进程握手/TUN)全断 + reload 可靠性簇拖累。

E. 已确认闭环(摘)

clash_api 全套(实测)、选择持久化、主流 outbound 装配、DNS 12 传输、SRS 解析、pre-swap rollback 事务(01B,9 测试实跑 PASS)、V2Ray 复用(01C)、协议解析 panic 安全(2493 unwrap 过滤后生产风险 = 0)、shutdown/reload 串行化。

F. 缺陷(30 项去重,全部 P0/核心 P1 经主线程逐条复核)

P0 ×3:
- CAL-01 GUI TUN 配置被 schema 拒绝(validator/v2/inbound.rs:8-34 无 mtu/address/stack)→ GUI 主模式启动即败
- CAL-02 启动字符串 sing-box started 缺失(GUI kernelApi.ts:257 硬依赖;Rust 全仓 0 匹配)→ 所有模式 GUI 判启动失败
- CAL-03 WireGuard endpoint 孤岛(EndpointAsOutbound 零调用)→ 硬启动失败(实测)

P1 ×7:CAL-04 reload 连续性簇(=种子 01A,三 agent 交叉确认被低估:提前关旧+无 diff+失败无恢复+信号竞态);CAL-05 inbound bind 失败不可见不致命(ready_tx 恒 None);CAL-06 主数据面零 liveness 监控;CAL-07 全局 registry pre-swap 安装、回滚不恢复(detour 热路径错位);CAL-08 lints deny 对 15/16 crate 是死配置;CAL-09 WG outbound 永 stub;CAL-10 TUN 数据面 skeleton。

P2 ×9 / P3 ×10:详见报告文件。

G. 推翻/降级

①种子 01A:DEFER→升级 P1;②ServiceManager 泄漏推论:DISPROVED(8 处补偿齐全);③"55/55⇒GUI-ready":推翻;④协议解析 panic 风险假设:DISPROVED;⑤SVC-V2RAY-API-01B/SIDECAR-POLICY-02A/TIDY-:维持;⑥"下一步=清 DEFER" 被推翻——未登记的 3 个 P0 比全部种子重要*。

H. 最危险未知(摘)

#1 GUI 实机从未拉起 Rust 内核(两个 P0 存活至今即证明);#2 GUI 切配置是重启进程还是热 reload(决定 CAL-04 暴露面);#4 strict schema 还拒绝多少 Go 合法字段(TUN 只是被暴露的第一个);#6 sled vs BoltDB cache.db 不互通。

I. 任务包(10 个)

PKG-GUI-CONTRACT-01(P0,小)/ PKG-SCHEMA-PARITY-01(P0,中)/ PKG-WG-DATAPLANE-01(P0,中)/ PKG-RELOAD-CONTINUITY-01(P1,大,高风险)/ PKG-INBOUND-LIVENESS-01 / PKG-TUN-DATAPLANE-01 / PKG-GUI-E2E-01(验证包)/ PKG-LINT-GATE-01(POLICY)/ PKG-HYGIENE-01 / PKG-DOC-CALIBRATE-01。

J. 推荐下一步

首选:PKG-GUI-CONTRACT-01 ∥ PKG-SCHEMA-PARITY-01 并行(成本最低、解锁 GUI 验证面)。RELOAD-CONTINUITY 先 probe(H-2/H-9)再动;TUN-DATAPLANE 等 schema 落地;LINT-GATE 等你拍板。不自动领取。

K. 最终状态

## main...origin/main          (零 tracked 修改、零 commit、零 push)
?? agents-only/a0_reality_spike/      (未触碰)
?? agents-only/fable5审计报告/         (本次授权产物,未提交)
HEAD = origin/main = 02d8d16e

审计完成,停止。