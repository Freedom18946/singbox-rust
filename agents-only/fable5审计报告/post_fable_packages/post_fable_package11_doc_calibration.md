<!-- tier: B -->
# post_fable_package11_doc_calibration

## Status

PLANNED.

## Source Findings

- CAL-26: external docs and capability ledgers are stale or misleading.

## Objective

Calibrate public and semi-public documentation so it no longer implies GUI readiness
or current capability truth that fable5 disproved.

## Implementation Contract

- Keep `agents-only/active_context.md` concise and authoritative for volatile state.
- Reconcile or archive stale docs such as `docs/TEST_EXECUTION_SUMMARY.md` and
  `docs/CLEANUP_COMPLETION_REPORT.md`.
- Update README claims that use unanchored performance/capability numbers, or add
  explicit source anchors and caveats.
- Regenerate or mark `reports/capabilities.json` with a current staleness/status
  banner.
- Ensure docs distinguish acceptance ledger closure from GUI drop-in readiness.

## Out Of Scope

- Product code fixes.
- Reopening closed REALITY/T3 lines.
- Changing the fable5 original audit report content.

## Acceptance Criteria

- External docs no longer present stale January/March status as current truth.
- README and capability references point to current authoritative context or clearly
  state their snapshot nature.
- `active_context.md` records post_fable package state only if a package changes the
  current recommended next step.

## Tests / Verification

- Run `rg` checks for stale phrases identified by fable5.
- Run `git diff --check`.
- If capability generation is used, run the generator and verify only intended docs
  change.

## Docs To Update

- README and stale docs/reports as required.
- `agents-only/active_context.md` only for current-state changes.
- This package file, under Completion Notes.

## Dependencies

- Can run anytime.
- Should avoid overwriting evidence generated by active implementation packages.

## Completion Notes

Not started.
