# L19.3.3 `sb-core` 重叠实现矩阵与第一波迁移计划

状态：✅ Completed（L19.3.3）  
日期：2026-03-04  
输入：`agents-only/03-planning/13-L19-REALITY-ALIGNMENT-WORKPACKAGES.md`（L19.3.3）

> 口径说明（2026-03-06 对齐）：
> - `L19.3.3` 是重叠分析与迁移来源编号。
> - 当前实际执行承载在 `L21 wave`，用于把 `MIG-01~MIG-06` 按小步提交持续落地。
> - `L18` 是项目总阶段，和当前 `L21 wave` 不冲突。

## 3DT. L21 wave#123 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W123-01 | MIG-02（OL-03 router/mod 解析失败 fallback 去 silent direct fallback） | ✅ done | `crates/sb-core/src/router/mod.rs`：将 `router_build_index_from_str(...)` 失败后的 6 处空索引默认值从 `default: "direct"` 调整为 `default: "unresolved"`，并把同步快照注释改为显式 unresolved 口径；补充无效规则 helper fallback 测试 | `owner.outbound.socks5` | `crates/sb-core/src/router/mod.rs` |
| W123-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.120-wave123-v1`（306 assertions），新增 W123 forbid/require 并产出 `wave123_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave123_v7_regression_block.txt` |

## 3DU. L21 wave#124 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W124-01 | MIG-02（OL-03 router/engine 兼容 RouterHandle caller default 收口） | ✅ done | `crates/sb-core/src/router/engine.rs`：新增 `compat_router_index_from_default(...)`，使兼容 `RouterHandle::new(router)` 不再忽略传入 `Router`，`replace(router)` 不再是 no-op；兼容 `Router::default()` 默认值从 silent direct fallback 收口到显式 `unresolved` | `owner.outbound.socks5` | `crates/sb-core/src/router/engine.rs` |
| W124-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.121-wave124-v1`（308 assertions），新增 W124 require，并产出 `wave124_v7_regression_block.txt`（注入兼容 helper/call-site 回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave124_v7_regression_block.txt` |

## 3DV. L21 wave#125 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W125-01 | MIG-02（OL-03 socks5-udp proxy NeedFallback 去 direct fallback） | ✅ done | `crates/sb-adapters/src/inbound/socks/udp.rs`：移除 legacy `SB_SOCKS_UDP_PROXY_FALLBACK_DIRECT` 兼容开关；`ProxyOutcome::NeedFallback` 改为显式告警并丢包，不再静默走 direct | `owner.outbound.socks5` | `crates/sb-adapters/src/inbound/socks/udp.rs` |
| W125-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.122-wave125-v1`（310 assertions），新增 W125 forbid/require，并产出 `wave125_v7_regression_block.txt`（注入旧 env 开关与告警回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave125_v7_regression_block.txt` |

## 3DW. L21 wave#126 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W126-01 | MIG-02（OL-03 shadowsocks inbound unsupported decision 去 direct fallback） | ✅ done | `crates/sb-adapters/src/inbound/shadowsocks.rs`：将 unsupported 路由决策（`Hijack/Sniff/Resolve/HijackDns`）从 `_ => direct` 收口为显式错误，禁止静默走 direct | `owner.outbound.socks5` | `crates/sb-adapters/src/inbound/shadowsocks.rs` |
| W126-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.123-wave126-v1`（312 assertions），新增 W126 forbid/require，并产出 `wave126_v7_regression_block.txt`（恢复旧注释/直连回退后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave126_v7_regression_block.txt` |

## 3DX. L21 wave#127 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W127-01 | MIG-02（OL-03 router_json 缺失 outbound 去 direct fallback） | ✅ done | `crates/sb-core/src/router/json_bridge.rs`：`JsonRule.outbound` 缺失时不再默认 `Decision::Direct`，改为显式 `Decision::Proxy(Some("unresolved"))`，并支持解析 `unresolved` marker | `owner.outbound.socks5` | `crates/sb-core/src/router/json_bridge.rs` |
| W127-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.124-wave127-v1`（314 assertions），新增 W127 forbid/require，并产出 `wave127_v7_regression_block.txt`（恢复 `unwrap_or(Decision::Direct)` 后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave127_v7_regression_block.txt` |

## 3DY. L21 wave#128 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W128-01 | MIG-02（OL-03 rules::from_rule_action 缺省动作去 direct fallback） | ✅ done | `crates/sb-core/src/router/rules.rs`：新增 `from_outbound_or_unresolved(...)`，使 `RuleAction::Route` 与 `RuleAction::RouteOptions` 缺失 `outbound` 时不再默认 `Decision::Direct`，统一改为显式 `Decision::Proxy(Some("unresolved"))`；补充最小单元测试锁定口径 | `owner.outbound.socks5` | `crates/sb-core/src/router/rules.rs` |
| W128-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.125-wave128-v1`（316 assertions），新增 W128 forbid/require，并产出 `wave128_v7_regression_block.txt`（恢复 `RouteOptions => Decision::Direct` 后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave128_v7_regression_block.txt` |

## 3DZ. L21 wave#129 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W129-01 | MIG-02（OL-03 naive inbound route-target kind 去 silent direct 标记） | ✅ done | `crates/sb-adapters/src/inbound/naive.rs`：新增 `decision_from_route_target(...)`，使 `OutboundKind::Socks/Http/Naive/Hysteria2` 等 kind 路由目标不再默默标记为 `Decision::Direct`，统一改为显式 `Decision::Proxy(Some(<kind>))`；补充最小单元测试锁定 route target 映射口径 | `owner.outbound.socks5` | `crates/sb-adapters/src/inbound/naive.rs` |
| W129-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.126-wave129-v1`（318 assertions），新增 W129 forbid/require，并产出 `wave129_v7_regression_block.txt`（恢复旧 `_ => Decision::Direct` 后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave129_v7_regression_block.txt` |

## 3EAA. L21 wave#130 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W130-01 | MIG-02（OL-03 hysteria2 inbound route-target kind 去 silent direct 标记） | ✅ done | `crates/sb-adapters/src/inbound/hysteria2.rs`：新增 `decision_from_route_target(...)`，使 `OutboundKind::Socks/Http/Naive/Hysteria2` 等 kind 路由目标不再默默标记为 `Decision::Direct`，统一改为显式 `Decision::Proxy(Some(<kind>))`；补充最小单元测试锁定 route target 映射口径 | `owner.outbound.socks5` | `crates/sb-adapters/src/inbound/hysteria2.rs` |
| W130-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.127-wave130-v1`（320 assertions），新增 W130 forbid/require，并产出 `wave130_v7_regression_block.txt`（恢复旧 `_ => Decision::Direct` 后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave130_v7_regression_block.txt` |

## 3EAB. L21 wave#131 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W131-01 | MIG-02（OL-03 tuic inbound route-target kind 去 silent direct 标记） | ✅ done | `crates/sb-adapters/src/inbound/tuic.rs`：新增 `decision_from_route_target(...)`，使 `OutboundKind::Socks/Http/Naive/Hysteria2` 等 kind 路由目标不再默默标记为 `Decision::Direct`，统一改为显式 `Decision::Proxy(Some(<kind>))`；补充最小单元测试锁定 route target 映射口径 | `owner.outbound.socks5` | `crates/sb-adapters/src/inbound/tuic.rs` |
| W131-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.128-wave131-v1`（322 assertions），新增 W131 forbid/require，并产出 `wave131_v7_regression_block.txt`（恢复旧 `_ => Decision::Direct` 后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave131_v7_regression_block.txt` |

## 3EAC. L21 wave#132 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W132-01 | MIG-02（OL-03 trojan inbound fallback 配置解析去 silent ignore） | ✅ done | `app/src/inbound_starter.rs`：新增 `parse_optional_inbound_fallback_addr(...)` 与 `parse_inbound_fallback_for_alpn(...)`，使 `start_trojan_inbound(...)` 不再静默吞掉无效 `fallback` / `fallback_for_alpn`，改为显式报错并拒绝启动；补充最小单元测试锁定显式报错口径 | `owner.outbound.socks5` | `app/src/inbound_starter.rs` |
| W132-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.129-wave132-v1`（324 assertions），新增 W132 require，并产出 `wave132_v7_regression_block.txt`（恢复旧 `parse(...).ok()` 后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave132_v7_regression_block.txt` |

## 3EAD. L21 wave#133 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W133-01 | MIG-02（OL-03 vmess inbound fallback 配置解析去 silent ignore） | ✅ done | `app/src/inbound_starter.rs`：`start_vmess_inbound(...)` 切到 `parse_optional_inbound_fallback_addr(...)` 与 `parse_inbound_fallback_for_alpn(...)`，不再静默吞掉无效 `fallback` / `fallback_for_alpn`，改为显式报错并拒绝启动；补充最小单元测试锁定 vmess helper 协议标签 | `owner.outbound.socks5` | `app/src/inbound_starter.rs` |
| W133-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.130-wave133-v1`（326 assertions），新增 W133 require，并产出 `wave133_v7_regression_block.txt`（恢复旧 `parse(...).ok()` 后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave133_v7_regression_block.txt` |

## 1. 盘点方法

1. 对比 `crates/sb-core/src/{inbound,outbound,transport}` 与 `crates/sb-adapters/src/{inbound,outbound}`、`crates/sb-transport/src` 的模块与类型定义。
2. 记录“同域能力重复实现”证据（文件 + 行号）。
3. 输出迁移优先级（风险/成本/影响）并指定单 owner 与目标 crate。

## 3DA. L21 wave#104 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W104-01 | MIG-02（OL-03 router_override 测试样例基础默认去 silent direct fallback） | ✅ done | ：router_override 测试样例基础默认去 silent direct fallback |  |  |
| W104-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 （268 assertions），新增 W104 forbid/require 并产出 （注入回流后  失败，exit_code=1） |  | ； |

## 3DB. L21 wave#105 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W105-01 | MIG-02（OL-03 router_select_ctx_meta 测试样例 default 去 silent direct fallback） | ✅ done | ：router_select_ctx_meta 测试样例 default 去 silent direct fallback |  |  |
| W105-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 （270 assertions），新增 W105 forbid/require 并产出 （注入回流后  失败，exit_code=1） |  | ； |

## 3DC. L21 wave#106 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W106-01 | MIG-02（OL-03 router_rules_port_range 测试样例 default 去 silent direct fallback） | ✅ done | ：router_rules_port_range 测试样例 default 去 silent direct fallback |  |  |
| W106-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 （272 assertions），新增 W106 forbid/require 并产出 （注入回流后  失败，exit_code=1） |  | ； |

## 3DD. L21 wave#107 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W107-01 | MIG-02（OL-03 router_explain 测试样例 default 去 silent direct fallback） | ✅ done | ：router_explain 测试样例 default 去 silent direct fallback |  |  |
| W107-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 （274 assertions），新增 W107 forbid/require 并产出 （注入回流后  失败，exit_code=1） |  | ； |

## 3DE. L21 wave#108 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W108-01 | MIG-02（OL-03 router_cache 测试样例 default 去 silent direct fallback） | ✅ done | ：router_cache 测试样例 default 去 silent direct fallback |  |  |
| W108-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 （276 assertions），新增 W108 forbid/require 并产出 （注入回流后  失败，exit_code=1） |  | ； |

---

## 2. 重叠实现矩阵（含 owner / target）

| ID | 重叠域 | `sb-core` 证据 | `sb-adapters`/`sb-transport` 证据 | 风险 | 成本 | 影响面 | Owner（唯一） | Target Crate |
|---|---|---|---|---|---|---|---|---|
| OL-01 | Outbound Direct | `crates/sb-core/src/outbound/direct.rs:12` `DirectOutbound` | `crates/sb-adapters/src/outbound/direct.rs:12` `DirectOutbound` | Medium | S | 路由出口路径 | `owner.outbound.direct` | `sb-adapters` |
| OL-02 | Outbound Block | `crates/sb-core/src/outbound/block.rs:10` `BlockOutbound` | `crates/sb-adapters/src/outbound/block.rs:8` `BlockOutbound` | Medium | S | 拦截/拒绝语义与错误口径 | `owner.outbound.block` | `sb-adapters` |
| OL-03 | Outbound SOCKS5 | `crates/sb-core/src/outbound/socks5.rs:23` `Socks5Outbound` | `crates/sb-adapters/src/outbound/socks5.rs:37` `Socks5Connector` | High | M | 鉴权/TCP+UDP/超时/ECH 行为 | `owner.outbound.socks5` | `sb-adapters` |
| OL-04 | Outbound Hysteria2 | `crates/sb-core/src/outbound/hysteria2.rs:76` `Hysteria2Outbound` | `crates/sb-adapters/src/outbound/hysteria2.rs:47` `Hysteria2Connector` | High | M | QUIC 握手、拥塞控制、鉴权路径 | `owner.outbound.hysteria2` | `sb-adapters` |
| OL-05 | Outbound Selector | `crates/sb-core/src/outbound/selector.rs:38` `Selector` | `crates/sb-adapters/src/outbound/selector.rs:10` `SelectorOutbound` | Medium | M | 组选择、健康检查、切换一致性 | `owner.outbound.selector` | `sb-core` |
| OL-06 | Inbound HTTP/Mixed | `crates/sb-core/src/inbound/http.rs:50` `HttpInboundService`；`crates/sb-core/src/inbound/mixed.rs:33` `MixedInbound` | `crates/sb-adapters/src/inbound/http.rs:145` `HttpProxyConfig`；`crates/sb-adapters/src/inbound/mixed.rs:32` `MixedInboundConfig` | High | L | GUI 常用入口、指标、路由接线 | `owner.inbound.http_mixed` | `sb-adapters` |
| OL-07 | Transport Dialer/TLS | `crates/sb-core/src/transport/dialer.rs:74` `Dialer`；`crates/sb-core/src/transport/tls.rs:11` `TlsClient` | `crates/sb-transport/src/dialer.rs:139` `Dialer`；`crates/sb-transport/src/tls.rs:62` `TlsDialer` | High | M | TLS/ECH、连接策略、错误分类 | `owner.transport.foundation` | `sb-transport` |

## 3DA. L21 wave#104 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W104-01 | MIG-02（OL-03 router_override 测试样例基础默认去 silent direct fallback） | ✅ done | ：router_override 测试样例基础默认去 silent direct fallback |  |  |
| W104-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 （268 assertions），新增 W104 forbid/require 并产出 （注入回流后  失败，exit_code=1） |  | ； |

## 3DB. L21 wave#105 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W105-01 | MIG-02（OL-03 router_select_ctx_meta 测试样例 default 去 silent direct fallback） | ✅ done | ：router_select_ctx_meta 测试样例 default 去 silent direct fallback |  |  |
| W105-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 （270 assertions），新增 W105 forbid/require 并产出 （注入回流后  失败，exit_code=1） |  | ； |

## 3DC. L21 wave#106 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W106-01 | MIG-02（OL-03 router_rules_port_range 测试样例 default 去 silent direct fallback） | ✅ done | ：router_rules_port_range 测试样例 default 去 silent direct fallback |  |  |
| W106-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 （272 assertions），新增 W106 forbid/require 并产出 （注入回流后  失败，exit_code=1） |  | ； |

## 3DD. L21 wave#107 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W107-01 | MIG-02（OL-03 router_explain 测试样例 default 去 silent direct fallback） | ✅ done | ：router_explain 测试样例 default 去 silent direct fallback |  |  |
| W107-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 （274 assertions），新增 W107 forbid/require 并产出 （注入回流后  失败，exit_code=1） |  | ； |

## 3DE. L21 wave#108 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W108-01 | MIG-02（OL-03 router_cache 测试样例 default 去 silent direct fallback） | ✅ done | ：router_cache 测试样例 default 去 silent direct fallback |  |  |
| W108-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 （276 assertions），新增 W108 forbid/require 并产出 （注入回流后  失败，exit_code=1） |  | ； |

---

## 3. 第一波迁移 backlog（P1，至少 5 项）

| Backlog ID | 对应重叠 | 优先级 | 目标 | 迁移动作 | Owner | Target |
|---|---|---|---|---|---|---|
| MIG-01 | OL-01 + OL-02 | P1 | 统一基础出站实现 | 保留 `sb-adapters` 的 Direct/Block；`sb-core` 改 thin wrapper（仅调用/编排） | `owner.outbound.direct` | `sb-adapters` |
| MIG-02 | OL-03 | P1 | SOCKS5 单实现 | 以 `sb-adapters/outbound/socks5.rs` 为主，`sb-core` 路径改桥接并移除重复握手逻辑 | `owner.outbound.socks5` | `sb-adapters` |
| MIG-03 | OL-04 | P1 | Hysteria2 单实现 | 统一到 `sb-adapters`，QUIC 公共能力下沉 `sb-transport` 公共 util | `owner.outbound.hysteria2` | `sb-adapters` |
| MIG-04 | OL-06 | P1 | HTTP/Mixed 入站单实现 | 以 `sb-adapters` 为唯一协议入口，`sb-core/inbound/*` 仅保留生命周期壳层或删除 | `owner.inbound.http_mixed` | `sb-adapters` |
| MIG-05 | OL-07 | P1 | 传输层单真源 | `Dialer/Tls` 以 `sb-transport` 为唯一实现；`sb-core/transport` 仅保留接口/桥接 | `owner.transport.foundation` | `sb-transport` |
| MIG-06 | OL-05 | P2 | 选择器职责收敛 | `Selector` 语义保留在 `sb-core`，`sb-adapters` 仅保留 trait 适配层 | `owner.outbound.selector` | `sb-core` |

## 3A. L20.3.1 波次 #1 执行状态（2026-03-05）

| Wave Item | 对应重叠 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W1-01 | OL-01 | ✅ done | `build_direct_outbound` 不再依赖 `sb_core::outbound::DirectConnector`；改为 `sb-adapters` 内 `CoreDirectConnector`（桥接 `outbound::direct::DirectOutbound`） | `owner.outbound.direct` | `crates/sb-adapters/src/register.rs` 变更可单文件 revert |
| W1-02 | OL-01（tailscale 直连兜底路径） | ✅ done | `TailscaleConnector` 的 `direct` 字段从 `Arc<DirectConnector>` 迁移为 `Arc<dyn sb_core::adapter::OutboundConnector>`，消除对 core 直连实现类型绑定 | `owner.outbound.direct` | `crates/sb-adapters/src/outbound/tailscale.rs` + `crates/sb-adapters/src/register.rs` 可双文件 revert |
| W1-03 | OL-07 | ✅ done | `sb-core` DoT 查询路径由 `crate::transport::tls::TlsClient` 迁到 `sb-transport::{TcpDialer,TlsDialer}` | `owner.transport.foundation` | `crates/sb-core/src/dns/dot.rs` 变更可单文件 revert |

> 说明：W1-02 为 W1-01 在 `tailscale` 分支的去 core-impl 绑定收敛，作为单独迁移项跟踪，避免 `direct` 实现迁移后 `tailscale` 路径回流到 `sb-core` 直连实现。

## 3B. L20.3.3 迁移后矩阵状态回填（2026-03-05）

| Backlog ID | 对应重叠 | 当前状态 | 依据 |
|---|---|---|---|
| MIG-01 | OL-01 + OL-02 | `closed` | wave#1~wave#4 + wave#22 已完成：W1/W2/W3 完成 direct 绑定迁移，wave#4 清理 `runtime/switchboard` 的 direct fallback 回流点，wave#22 进一步将 `bootstrap` selector/urltest 直连成员路径从 core `DirectConnector` 收敛到 adapter direct wrapper |
| MIG-02 | OL-03 | `in_progress` | L21 wave#5~wave#7 + wave#17~wave#127 已完成 app/tools/tests/runtime/bootstrap/switchboard/core-bridge/inbound-route/outbound-udp-balancer/api-route 多路径去重叠：`runtime/switchboard`、`app/bootstrap` 与 `core bridge` 已移除 core concrete SOCKS/HTTP/TUIC/VMess/VLESS/Shadowsocks/URLTest/ShadowTLS/Hysteria2/Trojan/SSH/Selector/Direct connector 构建，并对 fallback/已知分支补齐显式禁用迁移提示；core bridge 未知 outbound 类型、VLESS、Shadowsocks、URLTest、ShadowTLS、Hysteria2、TUIC、VMess、Trojan、SSH、Selector、Direct 以及 Block(no-scaffold) 分支均不再静默降级为 direct；`tools connect --outbound <name>`、默认 outbound/UDP 路径和 `v2ray test_route` 空 outbound_tag 场景均已禁用 direct fallback；HTTP CONNECT/SOCKS5 inbound route 缺失 outbound 不再回退 direct；SOCKS5 inbound UDP 缺失 UDP session 不再 fallback 到 NAT 直连；socks5-udp 与 socks5-udp-enhanced 的 unsupported decision 路径均不再按 direct 处理，socks5-udp 的 proxy 决策路径与 socks5-udp-enhanced 的 proxy 决策路径也均不再 fallback 到 direct；adapter bridge router rules text、routing engine default_outbound、conntrack inbound tcp outbound_tag、router analyze_rules 默认决策、router builder default_dec 默认决策、bootstrap router rules text rule_outbound 默认决策、env_dump udp_proxy_mode 默认决策、adapter bridge final_rule 默认决策、validator v2 outbound type 默认决策、router hot_reload validation 测试样例 default 默认决策、validator v2 unknown outbound type fallback 默认决策、bootstrap router rules text final default 默认决策、router mod default 默认决策、router analyze_fix 默认决策、sb-subscribe parse_singbox 默认决策、geosite demo default 示例、process routing demo default 示例、rule hot reload demo default 示例、router_rules_decide_with_meta 测试样例 default 默认值、router_suffix_strict 测试样例 default 默认值、router_resolver_async 测试样例 default 默认值、router_rules_include 测试样例 default 默认值、router_rules_index 测试样例 default 默认值、router_rules_reload_noop 测试样例 default 默认值、router_cache_transport 测试样例 default 默认值、router_rules_portset 测试样例 default 默认值、router_rules_file 测试样例 default 默认值与 router_summary 测试样例 default 默认值、router_override 测试样例 default 默认值、router_select_ctx_meta 测试样例 default 默认值、router_override 测试样例 default 默认值、router_select_ctx_meta 测试样例 default 默认值、router_rules_port_range 测试样例 default 默认值、router_rules_port_range 测试样例 default 默认值、router_explain 测试样例 default 默认值、router_explain 测试样例 default 默认值、router_cache 测试样例 default 默认值、router_domain_regex_matching 测试样例 default 默认值、router_priority 测试样例 default 默认值、router_cache 测试样例 default 默认值均不再 silent fallback 到 direct/default，改为显式 `unresolved`；http inbound 健康检查路径不再将 proxy 决策覆盖为 direct；http inbound 在 `RDecision::Proxy(Some/None)` 路径也不再隐式 fallback 到 default proxy/direct，且 `RDecision::Hijack/Sniff/Resolve/HijackDns` 也不再 direct fallback；socks5 inbound 在 `RDecision::Proxy(Some/None)` 路径也不再隐式 fallback 到 default proxy/direct，且 `RDecision::Hijack/Sniff/Resolve/HijackDns` 也不再 direct fallback；anytls inbound 在 `RDecision::Hijack/Sniff/Resolve/HijackDns` 路径也不再 direct fallback；vmess inbound、vless inbound、anytls inbound、shadowtls inbound、redirect inbound、tproxy inbound、shadowsocks inbound、trojan inbound 在 Proxy 决策路径均不再隐式 fallback 默认代理（含 `Proxy(None)`）；shadowsocks inbound 的 unsupported 决策路径（`Hijack/Sniff/Resolve/HijackDns`）也不再 `_ => direct`；router_json 缺失 `outbound` 时也不再默认 `Decision::Direct`，改为显式 `unresolved` marker；no-router stub 路径不再硬编码 direct 默认 outbound；router explain 无法提取 outbound 时不再推断 direct，也不再隐式推断 proxy，且 `derive_outbound` 不再使用 silent `default` fallback；router keyword static 决策在缺失 dec 时不再 silent `default`，改为显式 `unresolved`；router runtime 不再保留 `GLOBAL_PROXY` + `default_proxy()` 全局 fallback 状态入口；router rules 的 `rule_type/mode` 路径不再使用 silent 默认字面量（`default`/`and`），改为显式 `unresolved` 标记后再走统一匹配回退；UDP balancer 在 no-scaffold 与 no-upstream 场景也不再 fallback 到 direct；Bridge 的 `find_direct_fallback` helper 已删除；wave#123~wave#124 进一步收口 router/mod 解析失败 fallback 与 `router/engine.rs` 兼容 `RouterHandle::new/replace` 吞参与 no-op 路径，兼容默认值现显式尊重 caller-supplied default；wave#125 进一步删除 socks5-udp proxy upstream fallback 的 legacy direct 开关，NeedFallback 改为显式阻断与迁移提示；V7 断言持续固化防回流 |
| MIG-03 | OL-04 | `in_progress` | L21 wave#8 + wave#16 已在 `runtime/switchboard` 与 `app/bootstrap` selector/urltest 成员转换路径移除 core concrete Hysteria2 构建（统一显式 unsupported/disabled + 迁移指引），并以 V7 断言固化防回流 |
| MIG-04 | OL-06 | `in_progress` | L21 wave#9~wave#11 已完成 bridge + app/tests + examples 三路径去 core HTTP/Mixed concrete（`Bridge::new_from_config` 禁用 core inbound 实例化；`app/tests/inbound_http.rs` 与 `examples/code-examples/proxy/http_inbound_demo.rs` 迁移到 sb-adapters inbound）；V7 断言固化防回流 |
| MIG-05 | OL-07 | `closed` | wave#1~wave#4 已完成：DoT/diag/示例链路完成迁移，wave#4 收敛 app 非必要 `sb_core::transport` 引用并由 V7 断言固化 |
| MIG-06 | OL-05 | `closed` | L21 wave#12~wave#15 已完成 selector 路径收敛：测试/示例全面对齐 `SelectorGroup`，并新增 V7 断言禁止 `SelectorOutbound/UrlTestOutbound` concrete 回流（`l21.12-wave15-v1`），实现“语义在 sb-core，sb-adapters 仅适配组装”目标 |

## 3B.1. 下一阶段（wave100+）评估与预期

1. **当前剩余范围**：
   - `crates/sb-core/tests` 尚余 `23` 个文件、`50` 处 `default=direct` 样例字面量未完成收口。
2. **推荐排序**：
   - 先做单处样例文件：`router_rules_portset`、`router_cache_transport`、`router_process_rules_integration`、`router_select_ctx_meta`、`router_rules_file`、`router_override`、`router_summary`、`router_explain`、`router_inbound_outbound_tag_matching`、`router_rules_port_range`、`router_udp_rules`
   - 再做中等文件：`router_cache`、`router_domain_regex_matching`、`router_query_type_matching`、`router_ipversion_matching`、`router_rules_include_cycle`、`router_hot_reload_integration_complete`、`router_cidr4`、`router_rules_port_transport`、`router_priority`
   - 最后做高扰动文件：`router_geosite_rules_integration`、`router_hot_reload_integration`
3. **预期管理**：
   - 若保持当前“一波一文件 + 完整门禁 + 负样例阻断”的模板，剩余工作量保守估计仍需 `20+` 波
   - 高风险段不建议贪多合并，否则一旦断言语义变化，回滚与定位成本会明显上升
4. **完成判据**：
   - 这批剩余测试样例清零后，`MIG-02` 的剩余面将从“测试/示例默认值清债”转向“是否仍有真实运行路径的 direct fallback”

## 3C. L21 wave#2 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W2-01 | MIG-01（OL-01/OL-02 运行时接线收敛） | ✅ done | `populate_bridge_managers` 不再为每个 outbound 注入 `sb-core::DirectConnector` 占位；改为注册 bridge 实际 connector，阻断 `block/direct` 在 runtime manager 层被强制回流到 core 实现 | `owner.outbound.direct` | `crates/sb-core/src/runtime/supervisor.rs` |
| W2-02 | MIG-05（OL-07 传输链路收敛子项） | ✅ done | `app/src/bin/diag.rs` 的 TLS 诊断路径由 `sb_core::transport::tls::TlsClient` 迁到 `sb_transport::{TcpDialer,TlsDialer}`，并保留显式 SNI override | `owner.transport.foundation` | `app/src/bin/diag.rs` |

## 3D. L21 wave#3 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W3-01 | MIG-01（OL-01/OL-02 非核心路径去 `DirectConnector` 依赖） | ✅ done | `sb-api` 的 `add_outbound` 不再构造 `sb-core::DirectConnector`，改为复用 `outbound_manager.get("direct")`；`sb-adapters` 两处 TUN 测试改用本地 `DummyOutboundConnector`，阻断测试路径回流到 core concrete impl | `owner.outbound.direct` | `crates/sb-api/src/v2ray/services.rs`；`crates/sb-adapters/src/inbound/tun_process_aware.rs`；`crates/sb-adapters/tests/tun_process_integration.rs` |
| W3-02 | MIG-05（OL-07 transport 引用继续收敛） | ✅ done | `diag` 的 TCP 诊断路径从 `sb_core::transport::tcp::TcpDialer` 迁到 `sb_transport::TcpDialer` 并统一复用 `DialError` 分类；`tls_handshake` 与 `tcp_connect` 示例迁移到 `sb-transport` API | `owner.transport.foundation` | `app/src/bin/diag.rs`；`crates/sb-core/examples/tls_handshake.rs`；`examples/code-examples/network/tcp_connect.rs` |
| W3-03 | MIG-01/MIG-05 回流阻断门禁升级 | ✅ done | V7 allowlist 升级至 `l21.2-wave3-v1`（20 assertions），新增 W3 forbid/require 断言并生成负样例阻断证据 `wave3_v7_regression_block.txt`（注入回流后 `--v7-only` 失败） | `owner.outbound.direct` + `owner.transport.foundation` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave3_v7_regression_block.txt` |

## 3E. L21 wave#4 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W4-01 | MIG-01（OL-01/OL-02 runtime/switchboard fallback 清理） | ✅ done | `runtime/switchboard` 移除默认 direct connector 注入与 unknown→fallback 行为；`outbound/manager::resolve_default` 移除 direct 自动注入，空集合时返回显式诊断错误（含 requested/available） | `owner.outbound.direct` | `crates/sb-core/src/runtime/switchboard.rs`；`crates/sb-core/src/outbound/manager.rs`；`app/src/bootstrap.rs` |
| W4-02 | MIG-05（OL-07 剩余非必要 transport 引用收口） | ✅ done | `app/src/run_engine.rs` 迁移 transport plan 日志 target 从 `sb_core::transport` 到 `sb_transport`，与 wave#3 的 API 迁移口径一致 | `owner.transport.foundation` | `app/src/run_engine.rs` |
| W4-03 | MIG-01/MIG-05 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.3-wave4-v1`（29 assertions），新增 W4 forbid/require 并产出 `wave4_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.direct` + `owner.transport.foundation` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave4_v7_regression_block.txt` |

## 3F. L21 wave#5 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W5-01 | MIG-02（OL-03 app/tool 路径先行去重叠） | ✅ done | `app/src/bin/probe-outbound.rs` 不再使用 `runtime::switchboard::SwitchboardBuilder`，改为 `adapter::bridge::build_bridge + get_member`，并保留超时控制与原 CLI 输出语义 | `owner.outbound.socks5` | `app/src/bin/probe-outbound.rs` |
| W5-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.4-wave5-v1`（32 assertions），新增 W5 forbid/require 并产出 `wave5_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave5_v7_regression_block.txt` |

## 3G. L21 wave#6 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W6-01 | MIG-02（OL-03 app/tests 路径去 switchboard concrete 依赖） | ✅ done | `app/tests/{http_connect_inbound,socks_end2end,socks_via_selector,upstream_auth,upstream_socks_http}.rs` 由 `SwitchboardBuilder::from_config_ir` 改为 `OutboundSwitchboard::new()`，避免测试路径触发 core concrete SOCKS/HTTP connector 构建 | `owner.outbound.socks5` | 对应 5 个测试文件 |
| W6-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.5-wave6-v1`（42 assertions），新增 W6 forbid/require 并产出 `wave6_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave6_v7_regression_block.txt` |

## 3H. L21 wave#7 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W7-01 | MIG-02（OL-03 runtime/switchboard 去 core SOCKS concrete） | ✅ done | `SwitchboardBuilder::try_register_from_ir(OutboundType::Socks)` 不再构建 `socks_upstream::SocksUp`，改为显式 `UnsupportedProtocol` 提示迁移到 adapter bridge/supervisor；由 `from_config_ir` 统一降级为 `DegradedConnector` 保持行为可诊断 | `owner.outbound.socks5` | `crates/sb-core/src/runtime/switchboard.rs` |
| W7-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.6-wave7-v1`（44 assertions），新增 W7 forbid/require 并产出 `wave7_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave7_v7_regression_block.txt` |

## 3I. L21 wave#8 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W8-01 | MIG-03（OL-04 runtime/switchboard 去 core Hysteria2 concrete） | ✅ done | `SwitchboardBuilder::try_register_from_ir(OutboundType::Hysteria2)` 不再构建 `outbound::hysteria2::Hysteria2Outbound`，改为显式 `UnsupportedProtocol` 提示迁移到 adapter bridge/supervisor；由 `from_config_ir` 统一降级为 `DegradedConnector` | `owner.outbound.hysteria2` | `crates/sb-core/src/runtime/switchboard.rs` |
| W8-02 | MIG-03 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.7-wave8-v1`（46 assertions），新增 W8 forbid/require 并产出 `wave8_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.hysteria2` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave8_v7_regression_block.txt` |

## 3J. L21 wave#9 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W9-01 | MIG-04（OL-06 bridge 构建去 core HTTP/Mixed concrete） | ✅ done | `Bridge::new_from_config` 的 `InboundType::Http/Mixed` 不再构建 `inbound::http` 与 `inbound::mixed` core 实现；改为 `UnsupportedInbound` 并提示迁移到 `adapter::bridge::build_bridge`（sb-adapters inbound） | `owner.inbound.http_mixed` | `crates/sb-core/src/adapter/mod.rs` |
| W9-02 | MIG-04 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.8-wave9-v1`（50 assertions），新增 W9 forbid/require 并产出 `wave9_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.inbound.http_mixed` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave9_v7_regression_block.txt` |

## 3K. L21 wave#10 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W10-01 | MIG-04（OL-06 app/tests 去 core HTTP inbound concrete） | ✅ done | `app/tests/inbound_http.rs` 不再实例化 `sb_core::inbound::http::{HttpInboundService,HttpConfig}`，改为 `sb_adapters::inbound::http::{serve_http,HttpProxyConfig}` + `RouterHandle/OutboundRegistryHandle` 组装，统一通过 `start_http_inbound` 管理 ready/stop 生命周期 | `owner.inbound.http_mixed` | `app/tests/inbound_http.rs` |
| W10-02 | MIG-04 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.9-wave10-v1`（53 assertions），新增 W10 forbid/require 并产出 `wave10_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.inbound.http_mixed` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave10_v7_regression_block.txt` |

## 3L. L21 wave#11 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W11-01 | MIG-04（OL-06 examples 路径去 core HTTP inbound concrete） | ✅ done | `examples/code-examples/proxy/http_inbound_demo.rs` 不再使用 `singbox_rust::inbound::http::{HttpInbound,DirectConnector}`，改为 `sb_adapters::inbound::http::{serve_http,HttpProxyConfig}` + `RouterHandle/OutboundRegistryHandle` 组装并直接调用 `serve_http` | `owner.inbound.http_mixed` | `examples/code-examples/proxy/http_inbound_demo.rs` |
| W11-02 | MIG-04 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.10-wave11-v1`（56 assertions），新增 W11 forbid/require 并产出 `wave11_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.inbound.http_mixed` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave11_v7_regression_block.txt` |

## 3M. L21 wave#12 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W12-01 | MIG-06（OL-05 selector/urltest 测试路径架构对齐） | ✅ done | `app/tests/selector_udp_test.rs` 去除已删除的 `SelectorOutbound/UrlTestOutbound` concrete 类型依赖，统一改为直接使用 `sb_core::outbound::selector_group::SelectorGroup`（其已实现 `OutboundConnector + UdpOutboundFactory`），恢复 `cargo check -p app --tests` 编译通过 | `owner.outbound.selector` | `app/tests/selector_udp_test.rs` |
| W12-02 | MIG-06 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.11-wave12-v1`（59 assertions），新增 W12 forbid/require 并产出 `wave12_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.selector` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave12_v7_regression_block.txt` |

## 3N. L21 wave#13 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W13-01 | MIG-06（OL-05 测试编译稳定性清理） | ✅ done | `app/tests/protocol_chain_e2e.rs`：移除顶层未使用 `Arc` 导入，并将 `is_constrained_dial_error_str` 设为 `#[cfg(any(feature = \"shadowsocks\", feature = \"vmess\"))]`，消除默认特性下无效 dead_code/unused_import 预警 | `owner.outbound.selector` | `app/tests/protocol_chain_e2e.rs` |
| W13-02 | MIG-06 复验（不升级 allowlist） | ✅ done | 复验 `cargo check -p app --tests` 与 `check-boundaries --strict` 持续通过（V7 维持 `l21.11-wave12-v1`、59 assertions） | `owner.outbound.selector` | `reports/l21/artifacts/wave13_wp1_app_tests_check.txt`；`reports/l21/artifacts/wave13_strict_gate.txt` |

## 3O. L21 wave#14 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W14-01 | MIG-06（OL-05 测试告警收敛） | ✅ done | `app/src/analyze/registry.rs` 为 `supported_kinds/supported_async_kinds` 增加 `#[allow(dead_code)]`，消除 `app --tests` 目标下的既有 dead_code 告警 | `owner.outbound.selector` | `app/src/analyze/registry.rs` |
| W14-02 | MIG-06 复验（不升级 allowlist） | ✅ done | `cargo check -p app --tests`、`check-boundaries --strict`、`bash -n scripts/l18/gui_real_cert.sh` 持续通过（V7 维持 `l21.11-wave12-v1`、59 assertions） | `owner.outbound.selector` | `reports/l21/artifacts/wave14_wp1_app_tests_check.txt`；`reports/l21/artifacts/wave14_strict_gate.txt`；`reports/l21/artifacts/wave14_gui_static_syntax_check.txt` |

## 3P. L21 wave#15 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W15-01 | MIG-06（OL-05 回流阻断收口） | ✅ done | `agents-only/06-scripts/l20-migration-allowlist.txt` 升级到 `l21.12-wave15-v1`（63 assertions），新增 W15-01~W15-04：禁止 `sb-adapters` selector/urltest concrete 类型回流，并要求 builder 持续使用 core `SelectorGroup` | `owner.outbound.selector` | `agents-only/06-scripts/l20-migration-allowlist.txt` |
| W15-02 | MIG-06 strict gate 升级与阻断证据 | ✅ done | `check-boundaries --strict` 通过（`V7 PASS (63 assertions)`）；注入 `struct SelectorOutbound` 回流样例后 `--v7-only` 预期失败（`wave15_v7_regression_block.txt`, `exit_code=1`） | `owner.outbound.selector` | `reports/l21/artifacts/wave15_strict_gate.txt`；`reports/l21/artifacts/wave15_v7_regression_block.txt` |

## 3Q. L21 wave#16 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W16-01 | MIG-03（OL-04 bootstrap selector/urltest 去 core Hysteria2 concrete） | ✅ done | `app/src/bootstrap.rs` 的 `to_adapter_connector(OutboundImpl::Hysteria2)` 不再构建 `sb_core::outbound::hysteria2::Hysteria2Outbound`，改为显式 `warn + None` 并提示使用 adapter bridge/supervisor 路径 | `owner.outbound.hysteria2` | `app/src/bootstrap.rs` |
| W16-02 | MIG-03 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.13-wave16-v1`（65 assertions），新增 W16 forbid/require 并产出 `wave16_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.hysteria2` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave16_v7_regression_block.txt` |

## 3R. L21 wave#17 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W17-01 | MIG-02（OL-03 bootstrap selector/urltest 去 core SOCKS concrete） | ✅ done | `app/src/bootstrap.rs` 的 `to_adapter_connector(OutboundImpl::Socks5)` 不再构建 `socks_upstream::SocksUp`，改为显式 `warn + None` 并提示使用 adapter bridge/supervisor 路径 | `owner.outbound.socks5` | `app/src/bootstrap.rs` |
| W17-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.14-wave17-v1`（67 assertions），新增 W17 forbid/require 并产出 `wave17_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave17_v7_regression_block.txt` |

## 3S. L21 wave#18 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W18-01 | MIG-02（OL-03 bootstrap selector/urltest 去 core HTTP proxy concrete） | ✅ done | `app/src/bootstrap.rs` 的 `to_adapter_connector(OutboundImpl::HttpProxy)` 不再构建 `http_upstream::HttpUp`，改为显式 `warn + None` 并提示使用 adapter bridge/supervisor 路径 | `owner.outbound.socks5` | `app/src/bootstrap.rs` |
| W18-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.15-wave18-v1`（69 assertions），新增 W18 forbid/require 并产出 `wave18_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave18_v7_regression_block.txt` |

## 3T. L21 wave#19 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W19-01 | MIG-02（OL-03 bootstrap selector/urltest 去 core TUIC concrete） | ✅ done | `app/src/bootstrap.rs` 的 `to_adapter_connector(OutboundImpl::Tuic)` 不再构建 `outbound::tuic::TuicOutbound`，改为显式 `warn + None` 并提示使用 adapter bridge/supervisor 路径 | `owner.outbound.socks5` | `app/src/bootstrap.rs` |
| W19-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.16-wave19-v1`（71 assertions），新增 W19 forbid/require 并产出 `wave19_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave19_v7_regression_block.txt` |

## 3U. L21 wave#20 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W20-01 | MIG-02（OL-03 bootstrap selector/urltest 去 core VMess concrete） | ✅ done | `app/src/bootstrap.rs` 的 `to_adapter_connector(OutboundImpl::Vmess)` 不再构建 `outbound::vmess::VmessOutbound`，改为显式 `warn + None` 并提示使用 adapter bridge/supervisor 路径 | `owner.outbound.socks5` | `app/src/bootstrap.rs` |
| W20-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.17-wave20-v1`（73 assertions），新增 W20 forbid/require 并产出 `wave20_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave20_v7_regression_block.txt` |

## 3V. L21 wave#21 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W21-01 | MIG-02（OL-03 bootstrap selector/urltest 去 core VLESS concrete） | ✅ done | `app/src/bootstrap.rs` 的 `to_adapter_connector(OutboundImpl::Vless)` 不再构建 `outbound::vless::VlessOutbound`，改为显式 `warn + None` 并提示使用 adapter bridge/supervisor 路径 | `owner.outbound.socks5` | `app/src/bootstrap.rs` |
| W21-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.18-wave21-v1`（75 assertions），新增 W21 forbid/require 并产出 `wave21_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave21_v7_regression_block.txt` |

## 3W. L21 wave#22 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W22-01 | MIG-01（OL-01 bootstrap selector/urltest 去 core direct concrete） | ✅ done | `app/src/bootstrap.rs` 的 `to_adapter_connector(OutboundImpl::Direct)` 不再构建 `direct_connector::DirectConnector`，改为本地 `BootstrapDirectAdapterConnector`（内部委托 `sb_adapters::outbound::direct::DirectOutbound`） | `owner.outbound.direct` | `app/src/bootstrap.rs` |
| W22-02 | MIG-01 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.19-wave22-v1`（77 assertions），新增 W22 forbid/require 并产出 `wave22_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.direct` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave22_v7_regression_block.txt` |

## 3X. L21 wave#23 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W23-01 | MIG-02（OL-03 bootstrap selector/urltest Trojan 路径迁移提示补全） | ✅ done | `app/src/bootstrap.rs` 的 `to_adapter_connector(OutboundImpl::Trojan)` 从静默 `None` 调整为显式 `warn + None`，提示迁移到 adapter bridge/supervisor 路径 | `owner.outbound.socks5` | `app/src/bootstrap.rs` |
| W23-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.20-wave23-v1`（79 assertions），新增 W23 forbid/require 并产出 `wave23_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave23_v7_regression_block.txt` |

## 3Y. L21 wave#24 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W24-01 | MIG-02（OL-03 bootstrap selector/urltest fallback 去静默回退） | ✅ done | `app/src/bootstrap.rs` 的 `to_adapter_connector` 末尾 fallback 从静默 `_ => None` 升级为显式 `warn + None`，并携带统一迁移指引 | `owner.outbound.socks5` | `app/src/bootstrap.rs` |
| W24-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.21-wave24-v1`（81 assertions），新增 W24 forbid/require 并产出 `wave24_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave24_v7_regression_block.txt` |

## 3Z. L21 wave#25 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W25-01 | MIG-02（OL-03 bootstrap selector/urltest 已知分支显式化） | ✅ done | `app/src/bootstrap.rs` 的 `to_adapter_connector` 新增 `Block`、`Connector` 与 `Naive`（feature-gated）显式 `warn + None` 分支，减少兜底 fallback 命中面 | `owner.outbound.socks5` | `app/src/bootstrap.rs` |
| W25-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.22-wave25-v1`（83 assertions），新增 W25 require 并产出 `wave25_v7_regression_block.txt`（注入 `other => None` 后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave25_v7_regression_block.txt` |

## 3AA. L21 wave#26 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W26-01 | MIG-02（OL-03 switchboard 去 core HTTP upstream concrete） | ✅ done | `crates/sb-core/src/runtime/switchboard.rs` 的 `try_register_from_ir(OutboundType::Http)` 不再构建 `outbound::http_upstream::HttpUp`，改为显式 `UnsupportedProtocol` 并提示迁移到 adapter bridge/supervisor 路径 | `owner.outbound.socks5` | `crates/sb-core/src/runtime/switchboard.rs` |
| W26-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.23-wave26-v1`（85 assertions），新增 W26 forbid/require 并产出 `wave26_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave26_v7_regression_block.txt` |

## 3AB. L21 wave#27 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W27-01 | MIG-02（OL-03 core bridge 去 core HTTP/SOCKS upstream concrete） | ✅ done | `crates/sb-core/src/adapter/mod.rs` 的 `Bridge::new_from_config` 中，`OutboundType::Http/Socks` 不再构建 `outbound::http_upstream::HttpUp` 与 `outbound::socks_upstream::SocksUp`，统一改为 `UnsupportedOutboundConnector` 并提示迁移到 `adapter::bridge::build_bridge` | `owner.outbound.socks5` | `crates/sb-core/src/adapter/mod.rs` |
| W27-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.24-wave27-v1`（89 assertions），新增 W27 forbid/require 并产出 `wave27_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave27_v7_regression_block.txt` |

## 3AC. L21 wave#28 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W28-01 | MIG-02（OL-03 core bridge fallback 去静默 direct 回退） | ✅ done | `crates/sb-core/src/adapter/mod.rs` 的 `Bridge::new_from_config` 兜底分支由 `_ => direct_connector_fallback()` 调整为 `unsupported_outbound_connector(...)`，避免未知 outbound 类型静默降级到 direct | `owner.outbound.socks5` | `crates/sb-core/src/adapter/mod.rs` |
| W28-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.25-wave28-v1`（91 assertions），新增 W28 forbid/require 并产出 `wave28_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave28_v7_regression_block.txt` |

## 3AD. L21 wave#29 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W29-01 | MIG-02（OL-03 core bridge VLESS 去 direct fallback） | ✅ done | `crates/sb-core/src/adapter/mod.rs` 的 `Bridge::new_from_config` 中，`OutboundType::Vless` 由 `direct_connector_fallback()` 调整为 `unsupported_outbound_connector(...)`，避免 VLESS 分支静默降级到 direct | `owner.outbound.socks5` | `crates/sb-core/src/adapter/mod.rs` |
| W29-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.26-wave29-v1`（93 assertions），新增 W29 forbid/require 并产出 `wave29_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave29_v7_regression_block.txt` |

## 3AE. L21 wave#30 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W30-01 | MIG-02（OL-03 core bridge Shadowsocks 去 direct fallback） | ✅ done | `crates/sb-core/src/adapter/mod.rs` 的 `Bridge::new_from_config` 中，`OutboundType::Shadowsocks` 由 `direct_connector_fallback()` 调整为 `unsupported_outbound_connector(...)`，避免 Shadowsocks 分支静默降级到 direct | `owner.outbound.socks5` | `crates/sb-core/src/adapter/mod.rs` |
| W30-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.27-wave30-v1`（95 assertions），新增 W30 forbid/require 并产出 `wave30_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave30_v7_regression_block.txt` |

## 3AF. L21 wave#31 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W31-01 | MIG-02（OL-03 core bridge URLTest 去 direct fallback） | ✅ done | `crates/sb-core/src/adapter/mod.rs` 的 `Bridge::new_from_config` 中，`OutboundType::UrlTest` 由 `direct_connector_fallback()` 调整为 `unsupported_outbound_connector(...)`，避免 URLTest 分支静默降级到 direct | `owner.outbound.socks5` | `crates/sb-core/src/adapter/mod.rs` |
| W31-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.28-wave31-v1`（97 assertions），新增 W31 forbid/require 并产出 `wave31_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave31_v7_regression_block.txt` |

## 3AG. L21 wave#32 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W32-01 | MIG-02（OL-03 core bridge ShadowTLS 去 direct fallback） | ✅ done | `crates/sb-core/src/adapter/mod.rs` 的 `Bridge::new_from_config` 中，`OutboundType::Shadowtls` 由 `direct_connector_fallback()` 调整为 `unsupported_outbound_connector(...)`，避免 ShadowTLS 分支静默降级到 direct | `owner.outbound.socks5` | `crates/sb-core/src/adapter/mod.rs` |
| W32-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.29-wave32-v1`（99 assertions），新增 W32 forbid/require 并产出 `wave32_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave32_v7_regression_block.txt` |

## 3AH. L21 wave#33 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W33-01 | MIG-02（OL-03 core bridge Hysteria2 去 direct fallback） | ✅ done | `crates/sb-core/src/adapter/mod.rs` 的 `Bridge::new_from_config` 中，`OutboundType::Hysteria2` 由 `direct_connector_fallback()` 调整为 `unsupported_outbound_connector(...)`，避免 Hysteria2 分支静默降级到 direct | `owner.outbound.socks5` | `crates/sb-core/src/adapter/mod.rs` |
| W33-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.30-wave33-v1`（101 assertions），新增 W33 forbid/require 并产出 `wave33_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave33_v7_regression_block.txt` |

## 3AI. L21 wave#34 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W34-01 | MIG-02（OL-03 core bridge TUIC 去 direct fallback） | ✅ done | `crates/sb-core/src/adapter/mod.rs` 的 `Bridge::new_from_config` 中，`OutboundType::Tuic` 由 `direct_connector_fallback()` 调整为 `unsupported_outbound_connector(...)`，避免 TUIC 分支静默降级到 direct | `owner.outbound.socks5` | `crates/sb-core/src/adapter/mod.rs` |
| W34-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.31-wave34-v1`（103 assertions），新增 W34 forbid/require 并产出 `wave34_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave34_v7_regression_block.txt` |

## 3AJ. L21 wave#35 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W35-01 | MIG-02（OL-03 core bridge VMess 去 direct fallback） | ✅ done | `crates/sb-core/src/adapter/mod.rs` 的 `Bridge::new_from_config` 中，`OutboundType::Vmess` 由 `direct_connector_fallback()` 调整为 `unsupported_outbound_connector(...)`，避免 VMess 分支静默降级到 direct | `owner.outbound.socks5` | `crates/sb-core/src/adapter/mod.rs` |
| W35-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.32-wave35-v1`（105 assertions），新增 W35 forbid/require 并产出 `wave35_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave35_v7_regression_block.txt` |

## 3AK. L21 wave#36 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W36-01 | MIG-02（OL-03 core bridge Trojan 去 direct fallback） | ✅ done | `crates/sb-core/src/adapter/mod.rs` 的 `Bridge::new_from_config` 中，`OutboundType::Trojan` 由 `direct_connector_fallback()` 调整为 `unsupported_outbound_connector(...)`，避免 Trojan 分支静默降级到 direct | `owner.outbound.socks5` | `crates/sb-core/src/adapter/mod.rs` |
| W36-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.33-wave36-v1`（107 assertions），新增 W36 forbid/require 并产出 `wave36_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave36_v7_regression_block.txt` |

## 3AL. L21 wave#37 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W37-01 | MIG-02（OL-03 core bridge SSH 去 direct fallback） | ✅ done | `crates/sb-core/src/adapter/mod.rs` 的 `Bridge::new_from_config` 中，`OutboundType::Ssh` 由 `direct_connector_fallback()` 调整为 `unsupported_outbound_connector(...)`，避免 SSH 分支静默降级到 direct | `owner.outbound.socks5` | `crates/sb-core/src/adapter/mod.rs` |
| W37-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.34-wave37-v1`（109 assertions），新增 W37 forbid/require 并产出 `wave37_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave37_v7_regression_block.txt` |

## 3AM. L21 wave#38 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W38-01 | MIG-02（OL-03 core bridge Selector 去 direct fallback） | ✅ done | `crates/sb-core/src/adapter/mod.rs` 的 `Bridge::new_from_config` 中，`OutboundType::Selector` 由 `direct_connector_fallback()` 调整为 `unsupported_outbound_connector(...)`，避免 Selector 分支静默降级到 direct | `owner.outbound.socks5` | `crates/sb-core/src/adapter/mod.rs` |
| W38-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.35-wave38-v1`（111 assertions），新增 W38 forbid/require 并产出 `wave38_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave38_v7_regression_block.txt` |

## 3AN. L21 wave#39 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W39-01 | MIG-02（OL-03 core bridge Block no-scaffold 去 direct fallback） | ✅ done | `crates/sb-core/src/adapter/mod.rs` 的 `Bridge::new_from_config` 中，`OutboundType::Block` 的 no-scaffold 分支由 direct fallback 调整为 `unsupported_outbound_connector(...)`，避免 no-scaffold 变体静默降级到 direct | `owner.outbound.socks5` | `crates/sb-core/src/adapter/mod.rs` |
| W39-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.36-wave39-v1`（113 assertions），新增 W39 forbid/require 并产出 `wave39_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave39_v7_regression_block.txt` |

## 3AO. L21 wave#40 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W40-01 | MIG-02（OL-03 core bridge Direct 分支去 fallback helper） | ✅ done | `crates/sb-core/src/adapter/mod.rs` 的 `Bridge::new_from_config` 中，`OutboundType::Direct` 由 `direct_connector_fallback()` 调整为 `unsupported_outbound_connector(...)`，并删除 `direct_connector_fallback` helper，避免 direct fallback helper 回流 | `owner.outbound.socks5` | `crates/sb-core/src/adapter/mod.rs` |
| W40-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.37-wave40-v1`（116 assertions），新增 W40 forbid/require 并产出 `wave40_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave40_v7_regression_block.txt` |

## 3AP. L21 wave#41 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W41-01 | MIG-02（OL-03 tools named-outbound 去 direct fallback） | ✅ done | `app/src/cli/tools.rs` 的 `connect_tcp` 在 `outbound=Some(name)` 时不再执行 `.or_else(|| bridge.find_direct_fallback())`，改为显式报错 `requested outbound not found; direct fallback is disabled` | `owner.outbound.socks5` | `app/src/cli/tools.rs` |
| W41-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.38-wave41-v1`（118 assertions），新增 W41 forbid/require 并产出 `wave41_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave41_v7_regression_block.txt` |

## 3AQ. L21 wave#42 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W42-01 | MIG-02（OL-03 tools default-outbound 去 implicit direct fallback） | ✅ done | `app/src/cli/tools.rs` 的 `connect_tcp` 在 `outbound=None` 时不再调用 `find_direct_fallback()`，改为仅查找显式 `direct` 成员并在缺失时报错 `direct outbound not found; implicit direct fallback is disabled` | `owner.outbound.socks5` | `app/src/cli/tools.rs` |
| W42-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.39-wave42-v1`（120 assertions），新增 W42 forbid/require 并产出 `wave42_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave42_v7_regression_block.txt` |

## 3AR. L21 wave#43 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W43-01 | MIG-02（OL-03 HTTP CONNECT inbound route 去 direct fallback） | ✅ done | `crates/sb-core/src/inbound/http_connect.rs` 的 outbound 解析路径在缺失 detour 时不再 `outbound_tag=\"direct\" + find_direct_fallback()`，改为显式失败并输出 no-fallback 诊断 | `owner.outbound.socks5` | `crates/sb-core/src/inbound/http_connect.rs` |
| W43-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.40-wave43-v1`（123 assertions），新增 W43 forbid/require 并产出 `wave43_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave43_v7_regression_block.txt` |

## 3AS. L21 wave#44 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W44-01 | MIG-02（OL-03 SOCKS5 inbound route 去 direct fallback） | ✅ done | `crates/sb-core/src/inbound/socks5.rs` 的 outbound 解析路径在缺失 detour 时不再 `outbound_tag=\"direct\" + find_direct_fallback()`，改为显式失败并输出 no-fallback 诊断 | `owner.outbound.socks5` | `crates/sb-core/src/inbound/socks5.rs` |
| W44-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.41-wave44-v1`（126 assertions），新增 W44 forbid/require 并产出 `wave44_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave44_v7_regression_block.txt` |

## 3AT. L21 wave#45 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W45-01 | MIG-02（OL-03 UDP balancer 去 direct fallback） | ✅ done | `crates/sb-core/src/outbound/udp_balancer.rs` 的 no-scaffold 与 no-upstream 路径不再 fallback 到 direct，统一改为显式 no-fallback 失败诊断 | `owner.outbound.socks5` | `crates/sb-core/src/outbound/udp_balancer.rs` |
| W45-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.42-wave45-v1`（130 assertions），新增 W45 forbid/require 并产出 `wave45_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave45_v7_regression_block.txt` |

## 3AU. L21 wave#46 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W46-01 | MIG-02（OL-03 tools connect udp 去 direct fallback） | ✅ done | `app/src/cli/tools.rs` 的 `connect_udp` 在缺失 UDP factory 时不再 fallback 到 direct UDP socket，改为显式失败并输出 no-fallback 诊断 | `owner.outbound.socks5` | `app/src/cli/tools.rs` |
| W46-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.43-wave46-v1`（133 assertions），新增 W46 forbid/require 并产出 `wave46_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave46_v7_regression_block.txt` |

## 3AV. L21 wave#47 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W47-01 | MIG-02（OL-03 v2ray test_route 去 direct fallback） | ✅ done | `crates/sb-api/src/v2ray/services.rs` 的 `test_route` 在 `outbound_tag` 为空时不再回填 `direct`，改为显式 `failed_precondition` 并要求上层显式提供 outbound_tag | `owner.outbound.socks5` | `crates/sb-api/src/v2ray/services.rs` |
| W47-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.44-wave47-v1`（135 assertions），新增 W47 forbid/require 并产出 `wave47_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave47_v7_regression_block.txt` |

## 3AW. L21 wave#48 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W48-01 | MIG-02（OL-03 清理 Bridge direct fallback helper） | ✅ done | `crates/sb-core/src/adapter/mod.rs` 删除 `Bridge::find_direct_fallback()` helper（已无调用），收口 direct fallback helper 回流面 | `owner.outbound.socks5` | `crates/sb-core/src/adapter/mod.rs` |
| W48-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.45-wave48-v1`（136 assertions），新增 W48 forbid 并产出 `wave48_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave48_v7_regression_block.txt` |

## 3AX. L21 wave#49 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W49-01 | MIG-02（OL-03 SOCKS5 inbound UDP 去 direct/NAT fallback） | ✅ done | `crates/sb-core/src/inbound/socks5.rs` 的 UDP 路径在缺失 UDP session 时不再 fallback 到 NAT 直连，改为显式 no-fallback 迁移提示 | `owner.outbound.socks5` | `crates/sb-core/src/inbound/socks5.rs` |
| W49-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.46-wave49-v1`（139 assertions），新增 W49 forbid/require 并产出 `wave49_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave49_v7_regression_block.txt` |

## 3AY. L21 wave#50 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W50-01 | MIG-02（OL-03 no-router 默认 outbound 去 direct hardcode） | ✅ done | `crates/sb-core/src/inbound/http_connect.rs` 与 `crates/sb-core/src/inbound/socks5.rs` 的 no-router `Engine::decide` 不再硬编码 `direct`，改为 `resolve_default_outbound_tag()` 按配置选择具名 outbound（无候选则留空并走后续显式失败） | `owner.outbound.socks5` | `crates/sb-core/src/inbound/http_connect.rs`；`crates/sb-core/src/inbound/socks5.rs` |
| W50-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.47-wave50-v1`（143 assertions），新增 W50 forbid/require 并产出 `wave50_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave50_v7_regression_block.txt` |

## 3AZ. L21 wave#51 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W51-01 | MIG-02（OL-03 router explain 去 direct 默认推断） | ✅ done | `crates/sb-core/src/router/explain.rs` 的 `extract_outbound_from_reason` 在无法提取 outbound 时不再返回 `direct`，改为显式 `unresolved` 标记 | `owner.outbound.socks5` | `crates/sb-core/src/router/explain.rs` |
| W51-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.48-wave51-v1`（145 assertions），新增 W51 forbid/require 并产出 `wave51_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave51_v7_regression_block.txt` |

## 3BA. L21 wave#52 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W52-01 | MIG-02（OL-03 router explain 去 silent default fallback） | ✅ done | `crates/sb-core/src/router/explain.rs` 的 `derive_outbound` 从 `unwrap_or(\"default\")` 改为 `unwrap_or(\"unresolved\")`，避免 silent fallback 默认值语义 | `owner.outbound.socks5` | `crates/sb-core/src/router/explain.rs` |
| W52-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.49-wave52-v1`（147 assertions），新增 W52 forbid/require 并产出 `wave52_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave52_v7_regression_block.txt` |

## 3BB. L21 wave#53 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W53-01 | MIG-02（OL-03 router explain 去 proxy inference fallback） | ✅ done | `crates/sb-core/src/router/explain.rs` 的 `extract_outbound_from_reason` 删除 `reason.contains(\"proxy\")` 的隐式推断分支，无法解析时统一显式返回 `unresolved` | `owner.outbound.socks5` | `crates/sb-core/src/router/explain.rs` |
| W53-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.50-wave53-v1`（149 assertions），新增 W53 forbid/require 并产出 `wave53_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave53_v7_regression_block.txt` |

## 3BC. L21 wave#54 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W54-01 | MIG-02（OL-03 socks5-udp proxy 决策去 direct fallback） | ✅ done | `crates/sb-adapters/src/inbound/socks/udp.rs` 的 `RDecision::Proxy(_)` 分支不再 fallback 到 direct，改为显式 unsupported 警告 + 丢包，并记录 `proxy_unsupported` 指标 | `owner.outbound.socks5` | `crates/sb-adapters/src/inbound/socks/udp.rs` |
| W54-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.51-wave54-v1`（152 assertions），新增 W54 forbid/require 并产出 `wave54_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave54_v7_regression_block.txt` |

## 3BD. L21 wave#55 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W55-01 | MIG-02（OL-03 http inbound health fallback 去 direct override） | ✅ done | `crates/sb-adapters/src/inbound/http.rs` 的健康检查路径不再将 `RDecision::Proxy` 覆盖为 `RDecision::Direct`，改为显式 no-fallback 告警并保持 proxy 决策语义 | `owner.outbound.socks5` | `crates/sb-adapters/src/inbound/http.rs` |
| W55-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.52-wave55-v1`（155 assertions），新增 W55 forbid/require 并产出 `wave55_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave55_v7_regression_block.txt` |

## 3BE. L21 wave#56 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W56-01 | MIG-02（OL-03 vmess inbound proxy decision 去 implicit fallback） | ✅ done | `crates/sb-adapters/src/inbound/vmess.rs` 的 `RDecision::Proxy(Some)` 在 endpoint/pool/registry 不可用时不再 fallback 默认代理；`RDecision::Proxy(None)` 改为显式 unsupported + no-fallback 诊断，并删除 `fallback_connect` helper | `owner.outbound.socks5` | `crates/sb-adapters/src/inbound/vmess.rs` |
| W56-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.53-wave56-v1`（158 assertions），新增 W56 forbid/require 并产出 `wave56_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave56_v7_regression_block.txt` |

## 3BF. L21 wave#57 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W57-01 | MIG-02（OL-03 vless inbound proxy decision 去 implicit fallback） | ✅ done | `crates/sb-adapters/src/inbound/vless.rs` 的 `RDecision::Proxy(Some)` 在 endpoint/pool/registry 不可用时不再 fallback 默认代理；`RDecision::Proxy(None)` 改为显式 unsupported + no-fallback 诊断，并删除 `fallback_connect` helper | `owner.outbound.socks5` | `crates/sb-adapters/src/inbound/vless.rs` |
| W57-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.54-wave57-v1`（161 assertions），新增 W57 forbid/require 并产出 `wave57_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave57_v7_regression_block.txt` |

## 3BG. L21 wave#58 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W58-01 | MIG-02（OL-03 anytls inbound proxy decision 去 implicit fallback） | ✅ done | `crates/sb-adapters/src/inbound/anytls.rs` 的 `RDecision::Proxy(Some)` 在 endpoint/pool/registry 不可用时不再 fallback 默认代理；`RDecision::Proxy(None)` 改为显式 unsupported + no-fallback 诊断，并删除 `fallback_connect` helper | `owner.outbound.socks5` | `crates/sb-adapters/src/inbound/anytls.rs` |
| W58-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.55-wave58-v1`（164 assertions），新增 W58 forbid/require 并产出 `wave58_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave58_v7_regression_block.txt` |

## 3BH. L21 wave#59 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W59-01 | MIG-02（OL-03 shadowtls inbound proxy decision 去 implicit fallback） | ✅ done | `crates/sb-adapters/src/inbound/shadowtls.rs` 的 `RDecision::Proxy(Some)` 在 endpoint/pool/registry 不可用时不再 fallback 默认代理；`RDecision::Proxy(None)` 改为显式 unsupported + no-fallback 诊断，并删除 `fallback_connect` helper | `owner.outbound.socks5` | `crates/sb-adapters/src/inbound/shadowtls.rs` |
| W59-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.56-wave59-v1`（167 assertions），新增 W59 forbid/require 并产出 `wave59_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave59_v7_regression_block.txt` |

## 3BI. L21 wave#60 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W60-01 | MIG-02（OL-03 redirect inbound proxy decision 去 implicit fallback） | ✅ done | `crates/sb-adapters/src/inbound/redirect.rs` 的 `RDecision::Proxy(Some)` 在 endpoint/pool/registry 不可用时不再 fallback 默认代理；`RDecision::Proxy(None)` 改为显式 unsupported + no-fallback 诊断 | `owner.outbound.socks5` | `crates/sb-adapters/src/inbound/redirect.rs` |
| W60-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.57-wave60-v1`（170 assertions），新增 W60 forbid/require 并产出 `wave60_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave60_v7_regression_block.txt` |

## 3BJ. L21 wave#61 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W61-01 | MIG-02（OL-03 tproxy inbound proxy decision 去 implicit fallback） | ✅ done | `crates/sb-adapters/src/inbound/tproxy.rs` 的 `RDecision::Proxy(Some)` 在 endpoint/pool/registry 不可用时不再 fallback 默认代理；`RDecision::Proxy(None)` 改为显式 unsupported + no-fallback 诊断 | `owner.outbound.socks5` | `crates/sb-adapters/src/inbound/tproxy.rs` |
| W61-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.58-wave61-v1`（173 assertions），新增 W61 forbid/require 并产出 `wave61_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave61_v7_regression_block.txt` |

## 3BK. L21 wave#62 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W62-01 | MIG-02（OL-03 shadowsocks inbound proxy decision 去 implicit fallback） | ✅ done | `crates/sb-adapters/src/inbound/shadowsocks.rs` 的 `RDecision::Proxy(Some)` 在 endpoint/pool/registry 不可用时不再 fallback 默认代理；`RDecision::Proxy(None)` 改为显式 unsupported + no-fallback 诊断 | `owner.outbound.socks5` | `crates/sb-adapters/src/inbound/shadowsocks.rs` |
| W62-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.59-wave62-v1`（176 assertions），新增 W62 forbid/require 并产出 `wave62_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave62_v7_regression_block.txt` |

## 3BL. L21 wave#63 执行状态（2026-03-05）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W63-01 | MIG-02（OL-03 trojan inbound proxy decision 去 implicit fallback） | ✅ done | `crates/sb-adapters/src/inbound/trojan.rs` 的 `RDecision::Proxy(Some)` 在 endpoint/pool/registry 不可用时不再 fallback 默认代理；`RDecision::Proxy(None)` 改为显式 unsupported + no-fallback 诊断 | `owner.outbound.socks5` | `crates/sb-adapters/src/inbound/trojan.rs` |
| W63-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.60-wave63-v1`（179 assertions），新增 W63 forbid/require 并产出 `wave63_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave63_v7_regression_block.txt` |

## 3BM. L21 wave#64 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W64-01 | MIG-02（OL-03 http inbound proxy decision 去 implicit fallback） | ✅ done | `crates/sb-adapters/src/inbound/http.rs` 的 `RDecision::Proxy(Some)` 在 endpoint/pool/registry 不可用时不再 fallback 到 default proxy/direct；`RDecision::Proxy(None)` 改为显式 unsupported + no-fallback 诊断 | `owner.outbound.socks5` | `crates/sb-adapters/src/inbound/http.rs` |
| W64-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.61-wave64-v1`（182 assertions），新增 W64 forbid/require 并产出 `wave64_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave64_v7_regression_block.txt` |

## 3BN. L21 wave#65 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W65-01 | MIG-02（OL-03 socks5 inbound proxy decision 去 implicit fallback） | ✅ done | `crates/sb-adapters/src/inbound/socks/mod.rs` 的 `RDecision::Proxy(Some)` 在 endpoint/pool/registry 不可用时不再 fallback 到 default proxy/direct；`RDecision::Proxy(None)` 改为显式 unsupported + no-fallback 诊断，并返回 SOCKS `REP=0x01` | `owner.outbound.socks5` | `crates/sb-adapters/src/inbound/socks/mod.rs` |
| W65-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.62-wave65-v1`（185 assertions），新增 W65 forbid/require 并产出 `wave65_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave65_v7_regression_block.txt` |

## 3BO. L21 wave#66 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W66-01 | MIG-02（OL-03 router keyword static 去 silent default fallback） | ✅ done | `crates/sb-core/src/router/mod.rs` 的 `router_index_decide_keyword_static` 从 `unwrap_or(\"default\")` 改为 `unwrap_or(\"unresolved\")`，避免 silent default fallback 语义 | `owner.outbound.socks5` | `crates/sb-core/src/router/mod.rs` |
| W66-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.63-wave66-v1`（187 assertions），新增 W66 forbid/require 并产出 `wave66_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave66_v7_regression_block.txt` |

## 3BP. L21 wave#67 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W67-01 | MIG-02（OL-03 http inbound unsupported decision 去 direct fallback） | ✅ done | `crates/sb-adapters/src/inbound/http.rs` 的 `RDecision::Hijack/Sniff/Resolve/HijackDns` 分支不再 direct fallback，改为显式 unsupported + no-fallback 诊断 | `owner.outbound.socks5` | `crates/sb-adapters/src/inbound/http.rs` |
| W67-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.64-wave67-v1`（189 assertions），新增 W67 forbid/require 并产出 `wave67_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave67_v7_regression_block.txt` |

## 3BQ. L21 wave#68 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W68-01 | MIG-02（OL-03 socks5 inbound unsupported decision 去 direct fallback） | ✅ done | `crates/sb-adapters/src/inbound/socks/mod.rs` 的 `RDecision::Hijack/Sniff/Resolve/HijackDns` 分支不再 direct fallback，改为显式 unsupported + SOCKS `REP=0x01` | `owner.outbound.socks5` | `crates/sb-adapters/src/inbound/socks/mod.rs` |
| W68-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.65-wave68-v1`（191 assertions），新增 W68 forbid/require 并产出 `wave68_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave68_v7_regression_block.txt` |

## 3BR. L21 wave#69 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W69-01 | MIG-02（OL-03 anytls inbound unsupported decision 去 direct fallback） | ✅ done | `crates/sb-adapters/src/inbound/anytls.rs` 的 `RDecision::Hijack/Sniff/Resolve/HijackDns` 分支不再 direct fallback，改为显式 unsupported + no-fallback 诊断 | `owner.outbound.socks5` | `crates/sb-adapters/src/inbound/anytls.rs` |
| W69-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.66-wave69-v1`（193 assertions），新增 W69 forbid/require 并产出 `wave69_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave69_v7_regression_block.txt` |

## 3BS. L21 wave#70 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W70-01 | MIG-02（OL-03 router runtime 去 global default-proxy fallback 状态） | ✅ done | `crates/sb-core/src/router/runtime.rs` 删除 `GLOBAL_PROXY`、`init_default_proxy_from_env` 与 `default_proxy`，保留 `parse_proxy_from_env` 纯解析能力 | `owner.outbound.socks5` | `crates/sb-core/src/router/runtime.rs` |
| W70-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.67-wave70-v1`（195 assertions），新增 W70 forbid 并产出 `wave70_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave70_v7_regression_block.txt` |

## 3BT. L21 wave#71 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W71-01 | MIG-02（OL-03 router rules 去 silent default literal） | ✅ done | `crates/sb-core/src/router/rules.rs` 将 `rule_type` 的 `unwrap_or(\"default\")` 与 `mode` 的 `unwrap_or(\"and\")` 改为 `unwrap_or(\"unresolved\")`，去除 silent 默认字面量 | `owner.outbound.socks5` | `crates/sb-core/src/router/rules.rs` |
| W71-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.68-wave71-v1`（198 assertions），新增 W71 forbid/require 并产出 `wave71_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave71_v7_regression_block.txt` |

## 3BU. L21 wave#72 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W72-01 | MIG-02（OL-03 socks5-udp unsupported decision 去 direct fallback） | ✅ done | `crates/sb-adapters/src/inbound/socks/udp.rs` 的 `RDecision::Hijack/Sniff/Resolve/HijackDns` 分支不再按 direct 处理，改为显式告警 + 丢包 + `unsupported_no_fallback` 指标 | `owner.outbound.socks5` | `crates/sb-adapters/src/inbound/socks/udp.rs` |
| W72-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.69-wave72-v1`（201 assertions），新增 W72 forbid/require 并产出 `wave72_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave72_v7_regression_block.txt` |

## 3BV. L21 wave#73 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W73-01 | MIG-02（OL-03 socks5-udp enhanced unsupported decision 去 direct fallback） | ✅ done | `crates/sb-adapters/src/inbound/socks/udp_enhanced.rs` 的 `RDecision::Hijack/Sniff/Resolve/HijackDns` 分支不再按 direct 处理，改为显式告警 + 丢包 + `unsupported_no_fallback` 指标 | `owner.outbound.socks5` | `crates/sb-adapters/src/inbound/socks/udp_enhanced.rs` |
| W73-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.70-wave73-v1`（204 assertions），新增 W73 forbid/require 并产出 `wave73_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave73_v7_regression_block.txt` |

## 3BW. L21 wave#74 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W74-01 | MIG-02（OL-03 socks5-udp enhanced proxy decision 去 direct fallback） | ✅ done | `crates/sb-adapters/src/inbound/socks/udp_enhanced.rs` 的 `RDecision::Proxy(_)` 分支不再 direct fall-through；proxy send 失败与缺失 SOCKS5 upstream 场景均改为显式告警 + 丢包 + `proxy_no_fallback` 指标 | `owner.outbound.socks5` | `crates/sb-adapters/src/inbound/socks/udp_enhanced.rs` |
| W74-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.71-wave74-v1`（208 assertions），新增 W74 forbid/require 并产出 `wave74_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave74_v7_regression_block.txt` |

## 3BX. L21 wave#75 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W75-01 | MIG-02（OL-03 adapter bridge router rules text 去 silent direct fallback） | ✅ done | `crates/sb-core/src/adapter/bridge.rs` 的 `ir_to_router_rules_text::rule_outbound` 默认值由 `direct` 调整为 `unresolved`，避免 silent direct fallback 字面量 | `owner.outbound.socks5` | `crates/sb-core/src/adapter/bridge.rs` |
| W75-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.72-wave75-v1`（210 assertions），新增 W75 forbid/require 并产出 `wave75_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave75_v7_regression_block.txt` |

## 3BY. L21 wave#76 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W76-01 | MIG-02（OL-03 routing engine default_outbound 去 silent direct fallback） | ✅ done | `crates/sb-core/src/routing/engine.rs` 的 `default_outbound()` 默认值由 `direct` 调整为 `unresolved`，避免 silent direct fallback 字面量 | `owner.outbound.socks5` | `crates/sb-core/src/routing/engine.rs` |
| W76-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.73-wave76-v1`（212 assertions），新增 W76 forbid/require 并产出 `wave76_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave76_v7_regression_block.txt` |

## 3BZ. L21 wave#77 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W77-01 | MIG-02（OL-03 conntrack inbound tcp outbound_tag 去 silent direct fallback） | ✅ done | `crates/sb-core/src/conntrack/inbound_tcp.rs` 的 `with_outbound_tag(...)` 默认值由 `direct` 调整为 `unresolved`，避免 silent direct fallback 标签语义 | `owner.outbound.socks5` | `crates/sb-core/src/conntrack/inbound_tcp.rs` |
| W77-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.74-wave77-v1`（214 assertions），新增 W77 forbid/require 并产出 `wave77_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave77_v7_regression_block.txt` |

## 3CA. L21 wave#78 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W78-01 | MIG-02（OL-03 router analyze_fix 默认决策去 silent direct fallback） | ✅ done | `crates/sb-core/src/router/analyze_fix.rs` 的 `build_portrange_merge_patch` 在缺失决策标记时默认值由 `direct` 调整为 `unresolved`，避免 silent direct fallback 字面量 | `owner.outbound.socks5` | `crates/sb-core/src/router/analyze_fix.rs` |
| W78-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.75-wave78-v1`（216 assertions），新增 W78 forbid/require 并产出 `wave78_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave78_v7_regression_block.txt` |

## 3CB. L21 wave#79 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W79-01 | MIG-02（OL-03 sb-subscribe parse_singbox 默认决策去 silent default fallback） | ✅ done | `crates/sb-subscribe/src/parse_singbox.rs` 的 `map_rule` 在缺失 outbound 时默认决策由 `default` 调整为 `unresolved`，避免 silent default fallback 字面量 | `owner.outbound.socks5` | `crates/sb-subscribe/src/parse_singbox.rs` |
| W79-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.76-wave79-v1`（218 assertions），新增 W79 forbid/require 并产出 `wave79_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave79_v7_regression_block.txt` |

## 3CC. L21 wave#80 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W80-01 | MIG-02（OL-03 router analyze_rules 默认决策去 silent direct fallback） | ✅ done | `crates/sb-core/src/router/analyze.rs` 的 `analyze_rules` 在缺失 `to` 时默认决策由 `direct` 调整为 `unresolved`，避免 silent direct fallback 字面量 | `owner.outbound.socks5` | `crates/sb-core/src/router/analyze.rs` |
| W80-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.77-wave80-v1`（220 assertions），新增 W80 forbid/require 并产出 `wave80_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave80_v7_regression_block.txt` |

## 3CD. L21 wave#81 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W81-01 | MIG-02（OL-03 router builder default_dec 默认决策去 silent direct fallback） | ✅ done | `crates/sb-core/src/router/builder.rs` 的 `default_dec` 在缺失 `route.default/final_outbound` 时默认决策由 `direct` 调整为 `unresolved`，避免 silent direct fallback 字面量 | `owner.outbound.socks5` | `crates/sb-core/src/router/builder.rs` |
| W81-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.78-wave81-v1`（222 assertions），新增 W81 forbid/require 并产出 `wave81_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave81_v7_regression_block.txt` |

## 3CE. L21 wave#82 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W82-01 | MIG-02（OL-03 adapter bridge final_rule 默认决策去 silent direct fallback） | ✅ done | `crates/sb-core/src/adapter/bridge.rs` 的 `final_rule` 在缺失 `route.default/final_outbound` 时默认决策由 `direct` 调整为 `unresolved`，避免 silent direct fallback 字面量 | `owner.outbound.socks5` | `crates/sb-core/src/adapter/bridge.rs` |
| W82-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.79-wave82-v1`（224 assertions），新增 W82 forbid/require 并产出 `wave82_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave82_v7_regression_block.txt` |

## 3CF. L21 wave#83 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W83-01 | MIG-02（OL-03 bootstrap router rules text rule_outbound 默认决策去 silent direct fallback） | ✅ done | `app/src/bootstrap.rs` 的 `ir_to_router_rules_text` 在缺失 `outbound` 时默认决策由 `direct` 调整为 `unresolved`，避免 silent direct fallback 字面量 | `owner.outbound.socks5` | `app/src/bootstrap.rs` |
| W83-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.80-wave83-v1`（226 assertions），新增 W83 forbid/require 并产出 `wave83_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave83_v7_regression_block.txt` |

## 3CG. L21 wave#84 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W84-01 | MIG-02（OL-03 env_dump udp_proxy_mode 默认决策去 silent direct fallback） | ✅ done | `app/src/env_dump.rs` 的 `SB_UDP_PROXY_MODE` 缺失时默认值由 `direct` 调整为 `unresolved`，避免 silent direct fallback 字面量 | `owner.outbound.socks5` | `app/src/env_dump.rs` |
| W84-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.81-wave84-v1`（228 assertions），新增 W84 forbid/require 并产出 `wave84_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave84_v7_regression_block.txt` |

## 3CH. L21 wave#85 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W85-01 | MIG-02（OL-03 router mod default 默认决策去 silent direct fallback） | ✅ done | `crates/sb-core/src/router/mod.rs` 构建 `RouterIndex` 时 `default` 的默认决策由 `direct` 调整为 `unresolved`（两处），避免 silent direct fallback 字面量 | `owner.outbound.socks5` | `crates/sb-core/src/router/mod.rs` |
| W85-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.82-wave85-v1`（230 assertions），新增 W85 forbid/require 并产出 `wave85_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave85_v7_regression_block.txt` |

## 3CI. L21 wave#86 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W86-01 | MIG-02（OL-03 bootstrap router rules text final default 默认决策去 silent direct fallback） | ✅ done | `app/src/bootstrap.rs` 的 `ir_to_router_rules_text` 在缺失 `route.default` 时默认规则由 `default=direct` 调整为 `default=unresolved`，避免 silent direct fallback 字面量 | `owner.outbound.socks5` | `app/src/bootstrap.rs` |
| W86-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.83-wave86-v1`（232 assertions），新增 W86 forbid/require 并产出 `wave86_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave86_v7_regression_block.txt` |

## 3CJ. L21 wave#87 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W87-01 | MIG-02（OL-03 validator v2 outbound type 默认决策去 silent direct fallback） | ✅ done | `crates/sb-config/src/validator/v2.rs` 的 outbounds 类型解析中缺失 `type` 时默认值由 `direct` 调整为 `unresolved`，避免 silent direct fallback 字面量 | `owner.outbound.socks5` | `crates/sb-config/src/validator/v2.rs` |
| W87-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.84-wave87-v1`（234 assertions），新增 W87 forbid/require 并产出 `wave87_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave87_v7_regression_block.txt` |

## 3CK. L21 wave#88 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W88-01 | MIG-02（OL-03 validator v2 unknown outbound type fallback 去 silent direct fallback） | ✅ done | `crates/sb-config/src/validator/v2.rs` 的 outbounds 类型解析中未知 `type` 分支从 `_ => OutboundType::Direct` 调整为 `_ => OutboundType::Block`，移除 silent direct fallback | `owner.outbound.socks5` | `crates/sb-config/src/validator/v2.rs` |
| W88-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.85-wave88-v1`（236 assertions），新增 W88 forbid/require 并产出 `wave88_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave88_v7_regression_block.txt` |

## 3CL. L21 wave#89 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W89-01 | MIG-02（OL-03 router hot_reload validation 测试样例 default 去 silent direct fallback） | ✅ done | `crates/sb-core/src/router/hot_reload.rs` 的 `test_rule_set_validation` 中 `valid_content` 从 `default=direct` 调整为 `default=unresolved`，去除测试样例中的 silent direct fallback 字面量 | `owner.outbound.socks5` | `crates/sb-core/src/router/hot_reload.rs` |
| W89-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.86-wave89-v1`（238 assertions），新增 W89 forbid/require 并产出 `wave89_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave89_v7_regression_block.txt` |

## 3CM. L21 wave#90 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W90-01 | MIG-02（OL-03 dsl sample default 去 silent direct fallback） | ✅ done | `app/examples/dsl.sample.txt` 的默认规则从 `default=direct` 调整为 `default=unresolved`，去除示例中的 silent direct fallback 字面量 | `owner.outbound.socks5` | `app/examples/dsl.sample.txt` |
| W90-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.87-wave90-v1`（240 assertions），新增 W90 forbid/require 并产出 `wave90_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave90_v7_regression_block.txt` |

## 3CN. L21 wave#91 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W91-01 | MIG-02（OL-03 geosite demo default 去 silent direct fallback） | ✅ done | `crates/sb-core/examples/geosite_demo.rs` 的示例 fallback 从 `default=direct` 调整为 `default=unresolved`，去除示例中的 silent direct fallback 字面量 | `owner.outbound.socks5` | `crates/sb-core/examples/geosite_demo.rs` |
| W91-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.88-wave91-v1`（242 assertions），新增 W91 forbid/require 并产出 `wave91_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave91_v7_regression_block.txt` |

## 3CO. L21 wave#92 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W92-01 | MIG-02（OL-03 process routing demo default 去 silent direct fallback） | ✅ done | `crates/sb-core/examples/process_routing_demo.rs` 的示例 fallback 从 `default=direct` 调整为 `default=unresolved`，去除示例中的 silent direct fallback 字面量 | `owner.outbound.socks5` | `crates/sb-core/examples/process_routing_demo.rs` |
| W92-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.89-wave92-v1`（244 assertions），新增 W92 forbid/require 并产出 `wave92_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave92_v7_regression_block.txt` |

## 3CP. L21 wave#93 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W93-01 | MIG-02（OL-03 rule hot reload demo default 去 silent direct fallback） | ✅ done | `crates/sb-core/examples/rule_hot_reload_demo.rs` 的示例 fallback 从 `default=direct` 调整为 `default=unresolved`（两处），去除示例中的 silent direct fallback 字面量 | `owner.outbound.socks5` | `crates/sb-core/examples/rule_hot_reload_demo.rs` |
| W93-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.90-wave93-v1`（246 assertions），新增 W93 forbid/require 并产出 `wave93_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave93_v7_regression_block.txt` |

## 3CQ. L21 wave#94 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W94-01 | MIG-02（OL-03 router_rules_decide_with_meta 测试样例 default 去 silent direct fallback） | ✅ done | `crates/sb-core/tests/router_rules_decide_with_meta.rs` 的测试样例 fallback 从 `default=direct` 调整为 `default=unresolved`，去除示例中的 silent direct fallback 字面量 | `owner.outbound.socks5` | `crates/sb-core/tests/router_rules_decide_with_meta.rs` |
| W94-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.91-wave94-v1`（248 assertions），新增 W94 forbid/require 并产出 `wave94_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave94_v7_regression_block.txt` |

## 3CR. L21 wave#95 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W95-01 | MIG-02（OL-03 router_suffix_strict 测试样例 default 去 silent direct fallback） | ✅ done | `crates/sb-core/tests/router_suffix_strict.rs` 的测试样例 fallback 从 `default=direct` 调整为 `default=unresolved`，并将对应断言更新为 `unresolved`，去除示例中的 silent direct fallback 字面量 | `owner.outbound.socks5` | `crates/sb-core/tests/router_suffix_strict.rs` |
| W95-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.92-wave95-v1`（250 assertions），新增 W95 forbid/require 并产出 `wave95_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave95_v7_regression_block.txt` |

## 3CS. L21 wave#96 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W96-01 | MIG-02（OL-03 router_resolver_async 测试样例 default 去 silent direct fallback） | ✅ done | `crates/sb-core/tests/router_resolver_async.rs` 的测试样例 fallback 从 `default=direct` 调整为 `default=unresolved`，并将 timeout/error 默认回退断言更新为 `unresolved`，去除示例中的 silent direct fallback 字面量 | `owner.outbound.socks5` | `crates/sb-core/tests/router_resolver_async.rs` |
| W96-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.93-wave96-v1`（252 assertions），新增 W96 forbid/require 并产出 `wave96_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave96_v7_regression_block.txt` |

## 3CT. L21 wave#97 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W97-01 | MIG-02（OL-03 router_rules_include 测试样例 default 去 silent direct fallback） | ✅ done | `crates/sb-core/tests/router_rules_include.rs` 的测试样例 fallback 从 `default=direct` 调整为 `default=unresolved`，去除示例中的 silent direct fallback 字面量 | `owner.outbound.socks5` | `crates/sb-core/tests/router_rules_include.rs` |
| W97-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.94-wave97-v1`（254 assertions），新增 W97 forbid/require 并产出 `wave97_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave97_v7_regression_block.txt` |

## 3CU. L21 wave#98 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W98-01 | MIG-02（OL-03 router_rules_index 测试样例 default 去 silent direct fallback） | ✅ done | `crates/sb-core/tests/router_rules_index.rs` 的多处测试样例 fallback 从 `default=direct` 调整为 `default=unresolved`，并将无匹配默认断言更新为 `unresolved`，去除示例中的 silent direct fallback 字面量 | `owner.outbound.socks5` | `crates/sb-core/tests/router_rules_index.rs` |
| W98-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.95-wave98-v1`（256 assertions），新增 W98 forbid/require 并产出 `wave98_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave98_v7_regression_block.txt` |

## 3CV. L21 wave#99 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W99-01 | MIG-02（OL-03 router_rules_reload_noop 测试样例 default 去 silent direct fallback） | ✅ done | `crates/sb-core/tests/router_rules_reload_noop.rs` 的测试样例 fallback 从 `default=direct` 调整为 `default=unresolved`（两处），去除示例中的 silent direct fallback 字面量 | `owner.outbound.socks5` | `crates/sb-core/tests/router_rules_reload_noop.rs` |
| W99-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.96-wave99-v1`（258 assertions），新增 W99 forbid/require 并产出 `wave99_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave99_v7_regression_block.txt` |

## 3CW. L21 wave#100 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W100-01 | MIG-02（OL-03 router_cache_transport 测试样例 default 去 silent direct fallback） | ✅ done | `crates/sb-core/tests/router_cache_transport.rs` 的测试样例 fallback 从 `default=direct` 调整为 `default=unresolved`，去除示例中的 silent direct fallback 字面量 | `owner.outbound.socks5` | `crates/sb-core/tests/router_cache_transport.rs` |
| W100-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.97-wave100-v1`（260 assertions），新增 W100 forbid/require 并产出 `wave100_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave100_v7_regression_block.txt` |

## 3CX. L21 wave#101 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W101-01 | MIG-02（OL-03 router_rules_portset 测试样例 default 去 silent direct fallback） | ✅ done | `crates/sb-core/tests/router_rules_portset.rs` 的测试样例 fallback 从 `default=direct` 调整为 `default=unresolved`，并将默认命中断言更新为 `unresolved`，去除示例中的 silent direct fallback 字面量 | `owner.outbound.socks5` | `crates/sb-core/tests/router_rules_portset.rs` |
| W101-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.98-wave101-v1`（262 assertions），新增 W101 forbid/require 并产出 `wave101_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave101_v7_regression_block.txt` |

## 3CY. L21 wave#102 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W102-01 | MIG-02（OL-03 router_rules_file 测试样例 default 去 silent direct fallback） | ✅ done | `crates/sb-core/tests/router_rules_file.rs` 的测试样例 fallback 从 `default=direct` 调整为 `default=unresolved`，去除示例中的 silent direct fallback 字面量 | `owner.outbound.socks5` | `crates/sb-core/tests/router_rules_file.rs` |
| W102-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.99-wave102-v1`（264 assertions），新增 W102 forbid/require 并产出 `wave102_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave102_v7_regression_block.txt` |

## 3CZ. L21 wave#103 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W103-01 | MIG-02（OL-03 router_summary 测试样例 default 去 silent direct fallback） | ✅ done | `crates/sb-core/tests/router_summary.rs` 的测试样例 fallback 从 `default=direct` 调整为 `default=unresolved`，去除示例中的 silent direct fallback 字面量 | `owner.outbound.socks5` | `crates/sb-core/tests/router_summary.rs` |
| W103-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 `l21.100-wave103-v1`（266 assertions），新增 W103 forbid/require 并产出 `wave103_v7_regression_block.txt`（注入回流后 `--v7-only` 失败，exit_code=1） | `owner.outbound.socks5` | `agents-only/06-scripts/l20-migration-allowlist.txt`；`reports/l21/artifacts/wave103_v7_regression_block.txt` |

## 3DA. L21 wave#104 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W104-01 | MIG-02（OL-03 router_override 测试样例基础默认去 silent direct fallback） | ✅ done | ：router_override 测试样例基础默认去 silent direct fallback |  |  |
| W104-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 （268 assertions），新增 W104 forbid/require 并产出 （注入回流后  失败，exit_code=1） |  | ； |

## 3DB. L21 wave#105 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W105-01 | MIG-02（OL-03 router_select_ctx_meta 测试样例 default 去 silent direct fallback） | ✅ done | ：router_select_ctx_meta 测试样例 default 去 silent direct fallback |  |  |
| W105-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 （270 assertions），新增 W105 forbid/require 并产出 （注入回流后  失败，exit_code=1） |  | ； |

## 3DC. L21 wave#106 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W106-01 | MIG-02（OL-03 router_rules_port_range 测试样例 default 去 silent direct fallback） | ✅ done | ：router_rules_port_range 测试样例 default 去 silent direct fallback |  |  |
| W106-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 （272 assertions），新增 W106 forbid/require 并产出 （注入回流后  失败，exit_code=1） |  | ； |

## 3DD. L21 wave#107 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W107-01 | MIG-02（OL-03 router_explain 测试样例 default 去 silent direct fallback） | ✅ done | ：router_explain 测试样例 default 去 silent direct fallback |  |  |
| W107-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 （274 assertions），新增 W107 forbid/require 并产出 （注入回流后  失败，exit_code=1） |  | ； |

## 3DE. L21 wave#108 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W108-01 | MIG-02（OL-03 router_cache 测试样例 default 去 silent direct fallback） | ✅ done | ：router_cache 测试样例 default 去 silent direct fallback |  |  |
| W108-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 （276 assertions），新增 W108 forbid/require 并产出 （注入回流后  失败，exit_code=1） |  | ； |

---

## 4. 执行门禁建议（对接 L19.3.2）

1. 禁止新增 `sb-core` 协议实现文件（`inbound/*`、`outbound/*`）除非存在 ADR 例外。
2. 对 `OL-01~OL-07` 建立“迁移前后来源快照”日志：启动时打印实现来源（core/adapters/transport）。
3. 对每个 MIG 项要求：
   - 至少 1 条行为等价测试（连接/握手/错误码）。
   - 迁移后更新本矩阵状态（`open/in_progress/closed`）。

## 3DA. L21 wave#104 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W104-01 | MIG-02（OL-03 router_override 测试样例基础默认去 silent direct fallback） | ✅ done | ：router_override 测试样例基础默认去 silent direct fallback |  |  |
| W104-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 （268 assertions），新增 W104 forbid/require 并产出 （注入回流后  失败，exit_code=1） |  | ； |

## 3DB. L21 wave#105 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W105-01 | MIG-02（OL-03 router_select_ctx_meta 测试样例 default 去 silent direct fallback） | ✅ done | ：router_select_ctx_meta 测试样例 default 去 silent direct fallback |  |  |
| W105-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 （270 assertions），新增 W105 forbid/require 并产出 （注入回流后  失败，exit_code=1） |  | ； |

## 3DC. L21 wave#106 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W106-01 | MIG-02（OL-03 router_rules_port_range 测试样例 default 去 silent direct fallback） | ✅ done | ：router_rules_port_range 测试样例 default 去 silent direct fallback |  |  |
| W106-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 （272 assertions），新增 W106 forbid/require 并产出 （注入回流后  失败，exit_code=1） |  | ； |

## 3DD. L21 wave#107 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W107-01 | MIG-02（OL-03 router_explain 测试样例 default 去 silent direct fallback） | ✅ done | ：router_explain 测试样例 default 去 silent direct fallback |  |  |
| W107-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 （274 assertions），新增 W107 forbid/require 并产出 （注入回流后  失败，exit_code=1） |  | ； |

## 3DE. L21 wave#108 执行状态（2026-03-06）

| Wave Item | 对应迁移 | 状态 | 本轮落地 | Owner | 回滚点 |
|---|---|---|---|---|---|
| W108-01 | MIG-02（OL-03 router_cache 测试样例 default 去 silent direct fallback） | ✅ done | ：router_cache 测试样例 default 去 silent direct fallback |  |  |
| W108-02 | MIG-02 strict gate 升级与阻断证据 | ✅ done | V7 allowlist 升级至 （276 assertions），新增 W108 forbid/require 并产出 （注入回流后  失败，exit_code=1） |  | ； |

---

## 5. 验收映射（L19.3.3）

1. 已给出重叠矩阵（7 项）并覆盖 `sb-core` vs `sb-adapters/sb-transport`。
2. 第一波迁移 backlog 已给出 6 项（≥5）。
3. 每项均有唯一 owner 与目标 crate（见矩阵与 backlog 列）。
