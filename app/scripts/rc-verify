#!/usr/bin/env bash
set -euo pipefail

# Wrapper to call the workspace-level rc-verify helper so tests under app/ work.
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
WORKSPACE_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"
exec bash "${WORKSPACE_ROOT}/scripts/tools/release/rc-verify.sh" "$@"
