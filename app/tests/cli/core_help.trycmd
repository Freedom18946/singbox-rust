```console
$ app auth --help
鉴权相关工具[..]

Usage: app auth <COMMAND>

Commands:
  sign    生成签名
  replay  回放重试[..]
  help    Print this message or the help of the given subcommand(s)

Options:
  -h, --help[..]
  -V, --version[..]

$ app prom --help
Prometheus 指标工具[..]

Usage: app prom <COMMAND>

Commands:
  scrape  抓取并筛选样本
  hist    直方图转分位[..]
  help    Print this message or the help of the given subcommand(s)

Options:
  -h, --help[..]
  -V, --version[..]

$ app generate --help
Usage: app generate <COMMAND>

Commands:
  reality-keypair    Generate REALITY[..]
  ech-keypair        Generate ECH[..]
  tls-keypair        Generate a self-signed TLS[..]
  wireguard-keypair  Generate `WireGuard`[..]
  help               Print this message or the help of the given subcommand(s)

Options:
  -h, --help[..]
  -V, --version[..]

$ app gen-completions --help
Usage: app gen-completions[..]

Options:
      --shell <SHELL>[..]
      --dir <DIR>[..]
      --all[..]
  -h, --help[..]
  -V, --version[..]

$ app tools --help
[..]tools <COMMAND>

Commands:
  connect         Connect to an address and pipe stdin/stdout
  geodata-update  Download/update GeoIP/Geosite databases into a directory
  fetch           Fetch a URL (HTTP/HTTPS) and print body to stdout
  synctime        Query NTP server and print time offset
  fetch-http3     Fetch a URL using HTTP/3[..]
  help            Print this message or the help of the given subcommand(s)

Options:
  -h, --help[..]
  -V, --version[..]
```
