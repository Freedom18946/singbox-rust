id: p1_cli_ech_keypair_pem_format
title: generate ech-keypair produces PEM with ECH CONFIGS and ECH KEYS headers
priority: p1
kernel_mode: rust
env_class: strict
tags:
  - cli
  - generate
  - ech
owner: L15
bootstrap:
  rust:
    startup_timeout_ms: 5000
    ready_path: /
    api:
      base_url: "{{upstream.local_http}}"
gui_sequence: []
upstream_topology:
  - name: local_http
    kind: http_echo
    bind: 127.0.0.1:0
traffic_plan:
  - kind: command
    name: run_cli
    command: ./target/debug/app
    args:
      - generate
      - ech-keypair
      - test.example.com
    timeout_ms: 30000
    expect_exit: 0
assertions:
  - key: traffic.run_cli.detail.exit_code
    op: eq
    expected: 0
oracle:
  tolerate_counter_jitter: true
