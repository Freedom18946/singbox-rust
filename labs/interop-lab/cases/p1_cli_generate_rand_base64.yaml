id: p1_cli_generate_rand_base64
title: generate rand 16 --base64 produces 24-character base64 output
priority: p1
kernel_mode: rust
env_class: strict
tags:
  - cli
  - generate
owner: L15
bootstrap:
  rust:
    startup_timeout_ms: 5000
    ready_path: /
    api:
      base_url: https://example.com
gui_sequence: []
upstream_topology: []
traffic_plan:
  - kind: command
    name: run_cli
    command: ./target/debug/app
    args:
      - generate
      - rand
      - "16"
      - --base64
    timeout_ms: 30000
    expect_exit: 0
assertions:
  - key: traffic.run_cli.detail.exit_code
    op: eq
    expected: 0
oracle:
  tolerate_counter_jitter: true
