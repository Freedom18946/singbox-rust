# L20.3.1 Wave #1 执行记录

日期：2026-03-05  
范围：`L20.3.1`（重叠迁移波次 #1）

## 迁移项（3/3）

1. `W1-01`（OL-01）  
   - 变更：`crates/sb-adapters/src/register.rs`
   - 内容：`build_direct_outbound` 改为使用 adapter 内 `CoreDirectConnector`（桥接 `outbound::direct::DirectOutbound`），移除对 `sb_core::outbound::DirectConnector` 的直接依赖。
   - Owner：`owner.outbound.direct`
   - 回滚点：revert `crates/sb-adapters/src/register.rs`

2. `W1-02`（OL-01 tailscale 分支收敛）  
   - 变更：`crates/sb-adapters/src/outbound/tailscale.rs` + `crates/sb-adapters/src/register.rs`
   - 内容：`TailscaleConnector` 的 direct 依赖改为 `Arc<dyn sb_core::adapter::OutboundConnector>`，并复用 wave#1 的 direct bridge。
   - Owner：`owner.outbound.direct`
   - 回滚点：revert 上述两个文件

3. `W1-03`（OL-07）  
   - 变更：`crates/sb-core/src/dns/dot.rs`
   - 内容：DoT 查询路径从 `crate::transport::tls::TlsClient` 迁移到 `sb-transport::{TcpDialer,TlsDialer}`。
   - Owner：`owner.transport.foundation`
   - 回滚点：revert `crates/sb-core/src/dns/dot.rs`

## 最小验证

1. `cargo check -p sb-adapters`  
2. `cargo check -p sb-adapters --features "adapter-tailscale,legacy_tailscale_outbound"`  
3. `cargo check -p sb-core --features dns_dot,tls_rustls`  
4. `bash agents-only/06-scripts/check-boundaries.sh --strict`  

结果：全部通过（`check-boundaries --strict` 为 PASS）。
