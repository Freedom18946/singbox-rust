# Trojan server configuration template.
# Example only: replace password and certificate paths before deployment.
#
# Usage: app run -c configs/trojan-server.yaml

schema_version: 2
log:
  level: info
  output: /var/log/singbox-rust/singbox.log
  timestamp: true

inbounds:
  - type: trojan
    name: trojan-in
    listen: "0.0.0.0:443"

    # SECURITY: Change this to a strong password (>= 20 characters)
    users:
      - name: default
        password: CHANGEME_USE_STRONG_PASSWORD_HERE

    # Update paths to your TLS certificates
    cert: /etc/letsencrypt/live/your-domain.com/fullchain.pem
    key: /etc/letsencrypt/live/your-domain.com/privkey.pem

outbounds:
  - type: direct
    name: direct
  - type: block
    name: block

route:
  default: direct
