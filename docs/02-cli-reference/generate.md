# `generate` Command

Generate key material and helper artifacts.

## Usage

```bash
singbox-rust generate reality-keypair
singbox-rust generate ech-keypair
singbox-rust generate wireguard-keypair
singbox-rust generate uuid
singbox-rust generate tls-cert --domain example.com --out-cert cert.pem --out-key key.pem
```
