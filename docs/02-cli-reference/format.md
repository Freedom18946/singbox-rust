# `format` Command

Format and normalize configuration files.

## Usage

```bash
cargo run -p app -- format -c config.yaml
cargo run -p app -- format -c config.yaml -w
```

## Common Options

- `-w, --write`: write back to file
- `--indent <NUM>`: indentation size
