# CLI Tools Handshake

Integration notes for `tools` subcommands.

- All tools can be routed through config with `-c <config>`.
- Use `--outbound <TAG>` to force a specific outbound.
- Prefer `--timeout` for deterministic tests.
