# `rule-set` Command

Compile, validate, convert, and merge rule-set files.

## Usage

```bash
singbox-rust rule-set compile rules.json --out rules.srs
singbox-rust rule-set validate rules.srs
singbox-rust rule-set merge a.srs b.srs --out merged.srs
```
