{
  "dns": {
    "strategy": "prefer_ipv6",
    "rules": [
      { "geosite": "cn", "server": "g" },
      { "domain_suffix": "example", "server": "cf" }
    ],
    "servers": [
      { "type": "udp",   "server": "8.8.8.8",                 "tag": "g",    "detour": "direct" },
      { "type": "https", "server": "cloudflare-dns.com",       "tag": "cf" }
    ]
  }
}
