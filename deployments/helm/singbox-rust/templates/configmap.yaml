apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "singbox-rust.fullname" . }}-config
  labels:
    {{- include "singbox-rust.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: |
    {{- $value | nindent 4 }}
  {{- end }}
